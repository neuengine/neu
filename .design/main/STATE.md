# Project State

<!-- STATE.md — live project memory. Read FIRST in every workflow session. -->
<!-- Maximum 100 lines. Agent updates AFTER each completed action. -->

**Workspace:** main
**Updated:** 2026-06-03 14:25
**Phase:** 6 — UI, Tooling & Quality
**Status:** Active — App-integration cohort done + 5 editor families Stable (76→86). **T-6D04.1 UI interaction stack done** (correcting the earlier "no Todos left" claim — it was wrong: T-6D04.1 was actionable). **Remaining Phase 6 Todos:** T-6M02 (codegen boilerplate, deferred), T-6T01–05 (Bootstrap validation: plugin SDK contract / aiapi parity / CLI integration / codegen golden / live-ai smoke), T-6D04.2 (font atlas — gated). **User-gated (not Todos):** ws-dep ADR + `x/image/font` ADR (T-6X04). `asset-formats` Draft (glTF-gated).

## Current Position

- **Task:** "продолжи по порядку" → resumed Phase 6 execution. **T-6D04.1 UI interaction stack DONE** (the font-free half of T-6D04, split off like T-6A03/T-6P04). `internal/ui/system.go` `InteractionPlugin` (opt-in) adds one `PreUpdate` `ui.Interaction` system: `solveLayout` builds a `LayoutNode` tree from `Style` entities (linked via `hierarchy.Children`, ordered; roots = no UI-node parent), `Solve`s each root vs a `UIViewport` resource, writes each node's `LayoutRect` back; `updateInteraction` hit-tests `CursorPosition` vs those rects (painter Z-order + `MouseFilter` via existing `HitTest`), writes `Interaction`, and on fresh left-press moves the sole `Focused` marker (prior holder cleared via a `focusState` resource). **Design:** ONE combined system guarantees layout-before-hit-test without intra-schedule ordering assumptions; layout hands rects to interaction in-memory (no same-frame re-query); two-phase writes avoid query-mid-mutation. Built on the T-6D01..03 pure primitives — the deferred App-wiring. **Found+filled a foundation gap:** no prior task wrote a layout *system* (T-6D01 built only the tree `Solve` fn), so per-entity `LayoutRect`s didn't exist — the interaction system needed that bridge (same pattern as T-6A03's unbuilt instantiation pipeline). **internal/ui 91.8% cov** (system.go Build/solveLayout/isRoot/setFocus 100%, updateInteraction 92%). default `go test ./...` **75 pkgs green**; vet+modernize clean; own files gofmt-clean (pre-existing `layout.go` CRLF untouched, out of scope); `-race` in CI (CGO absent).
- **Next Action:** continue Phase 6 in order — next actionable Todo is **T-6M02** (codegen boilerplate: component-registration stubs + plugin scaffolds for `cmd/codegen/`; Track F `scaffold` already emits basic stubs, this extends it) or the **T-6T01–05 Bootstrap validation** tasks (now buildable since the underlying SDK/aiapi/CLI features exist). T-6D04.2 (font atlas) + the ws transport stay **user-gated** behind the `x/image/font` + ws-dep ADRs. Phase 6 still can't be closed (these Todos remain).

## Progress

```
Phase 1: [27/27] ████████ 100% ✓ Done
Phase 2: [24/24] ████████ 100% ✓ Done
Phase 3: [18/18] ████████ 100% ✓ Done
Phase 4: [19/19] ████████ 100% ✓ Done  ← all 10 specs Stable (render-core ratified)
Phase 5: [18/18] ████████ 100% ✓ Done  ← 10/10 specs Stable (asset-formats narrowed → Stable 2026-05-31)
Phase 6: [17/17] ████████ 100% ✓ engine-core cohort — 12 specs Stable (C29 gate closed)
Overall: [123/123] ████████ 100% 🎉 (engine-core cohort; P6 tooling+editor tracks deferred)
```

<!-- Phase 6 engine-core cohort = 17 tasks (A:3 B:2 C:3 D:3 G:2 K:3 + T-6T06) ALL DONE. Full P6 also has deferred tooling (E/F/I/J/L/M) + editor (H/N/O/P) tracks — not in the activated cohort. -->

## Recent Decisions

<!-- Last 3-5 locked decisions. Older entries → archived to PLAN.md -->

- 2026-06-03 **"продолжи по порядку" — T-6D04.1 UI interaction stack (font-free half)** — Resuming Phase 6 in order, found that "close Phase 6" was premature (9 Todos remained — my earlier "no Todos" note was wrong) and the first actionable, non-user-gated task was T-6D04's interaction half. Building it surfaced a **foundation gap** (the T-6A03 pattern): T-6D01..03 built only the *pure* UI primitives (`Solve` tree fn, `HitTest`, components) — **no layout *system*** ever wrote per-entity `LayoutRect`s, so the interaction system had nothing to hit-test. So T-6D04.1 = a layout ECS system + the interaction system. `internal/ui/system.go` `InteractionPlugin` (opt-in, NOT in DefaultPlugins) adds ONE `PreUpdate` `ui.Interaction` system: `solveLayout` builds a `LayoutNode` tree from `Style` entities (children via `hierarchy.Children`, order preserved; root = no UI-node parent), `Solve`s each root vs a `UIViewport` resource, writes `LayoutRect` back, and **hands the laid rects in-memory** to `updateInteraction` (no same-frame re-query) which hit-tests `CursorPosition` (painter Z-order + `MouseFilter` via the existing `HitTest`), writes `Interaction`, and on a fresh left-press moves the sole `Focused` marker (prior holder cleared via a `focusState` resource). **Key calls:** ONE combined system (not two) makes layout-precedes-hit-test true without relying on intra-schedule order; two-phase writes (collect → Insert) avoid mutating archetypes mid-query; the font-atlas glyph upload is split out as **T-6D04.2, gated on the `x/image/font` ADR (T-6X04)** — the interaction half is genuinely not font-gated. **internal/ui 91.8% cov** (flat + hierarchy layout, hover/press/miss/MouseIgnore/no-cursor/empty, focus transfer, plugin build; system.go solveLayout/isRoot/setFocus/Build 100%, updateInteraction 92%). default `go test ./...` **75 pkgs green**; vet + modernize clean; own files gofmt-clean (`internal/ui/layout.go` CRLF flag pre-existing + untouched, out of scope); `-race` in CI (CGO absent locally). **Handoff:** continue Phase 6 — T-6M02 (codegen) or T-6T01–05 (Bootstrap validation); T-6D04.2 + ws transport stay user-ADR-gated.
- 2026-06-03 **`/magic.task` — 5 editor-feature families promoted Draft→Stable (App-integration cohort dividend)** — With the cohort complete, ran Pre-Planning Stabilization on the 5 held editor families, **reconciling each L1+L2 against built code before promoting** (the C24 overclaim discipline, asset-formats lesson). Promoted: **cli-tooling**, **plugin-distribution**, **ai-api-plugin**, **ai-assistant-system**, **visual-graph** (L1+L2 each — 10 specs). **Two required narrowing** (the spec headline claimed more than is built): `l2-ai-assistant-system-go` claimed "stdio/ws/http" but `pkg/assistant` ships only stdio+http (no websocket.go) → narrowed ws to **ADR-gated** (C-003); `l2-ai-api-plugin-go` claimed "env/keyring/age" but `credentials.go` returns an explicit "ADR pending" error for keyring/file → narrowed to **env realized, keyring/age ADR-gated**. **Decision (user delegated "решай сам, не в ущерб качеству"):** narrow-then-promote over hold — chosen for **quality via consistency**: ai-assistant is structurally identical to ai-api (a complete realized surface + one ADR-gated extension), so holding one while promoting the other would be inconsistent; and promoting ai-assistant removes the **Stable(aiapi)→Draft(assistant) coupling** wart, since `pkg/plugins/aiapi` couples only to assistant's *realized* Method/Connection contract, not the deferred ws bit. visual-graph promotes because the engine wiring its v0.2.0 explicitly deferred is now built (T-6P04·rem). All file headers + changelog rows + INDEX rows updated atomically; pre-flight header-parity **ok** post-promotion. INDEX v2.42→**v2.43** (Stable **76→86**, Draft 36→26); PLAN/TASKS 1.28→**1.29**; RULES parity v1.8.0. **No phase/task structural change.** **Next (user decision):** close Phase 6 (Retro L2) vs. open the ws-dep / `x/image/font` ADRs vs. glTF for asset-formats.
- 2026-06-03 **`/magic.run` — T-6T07 App-integration validation (closes the App-integration cohort)** — Built `examples/editor/` (separate module mirroring `examples/diagnostic`/`ui`: own go.mod + `replace … => ../..`, added to `go.work`; examples that import `internal/` are the established pattern) as the cohort's end-to-end proof. **The crux was determinism:** the engine loop runs on the real wall clock — `gametime` feeds `DeltaSeconds` into the diagnostics and the live `grapheditor.Recorder` stamps `time.Now` — so hashing those would flap the C29 golden. Discipline: **run the real wall-clock loop but hash only deterministic facts** (registered diag paths, entity counts, plugin lifecycle state, UI rects, and graph frames seeded with *fixed* timestamps); the nondeterministic live `RunTraced→Recorder` path is validated by *structure* in a separate test, not by hash. `run() (uint64, error)` builds **one headless `app.App`** wiring **window** (`window.SyncPlugin` headless backend composes + ticks), **diagnostics** (`diag.DiagnosticsPlugin` + reader-gate INV-1), and the **graph-debug sync** plugin, **loads a first-party plugin in-process** (compile-time factory → `LoadInProcess` drives Build→Ready; `FinishInProcess` drives Finish→Cleanup; asserts `StateActive`→`StateDisabled`), exercises **definition hot-reload** (`pkgdef.Decode`→`InstanceStore.Instantiate`→`Reload` a modified scene, asserting the prior instance's entities are despawned — exactly the operation the hot-reload system performs on an asset `Modified` event), solves a **UI layout** tree, and runs the **graph-debug round-trip**: a fixed-timestamp trace drained by the `PostUpdate` `grapheditor.DebugSync` system through one real App frame → three `pkg/protocol` messages (trace/breakpoint/error) → each asserted to survive an `Encode → Decode` wire round-trip (`reflect.DeepEqual`, with float64 pin values so JSON num-coercion round-trips), wire bytes folded into the hash. `main_test.go`: `TestEditorHashStable` (≥20-run determinism, the C29 invariant) + `TestGraphTraceLivePath` (the live `Interpreter.RunTraced → grapheditor.Recorder → DebugStore` runtime path). **Golden `editor=13803481501758845285`** committed to `examples/goldens.json`; `cmd/examplecheck` reports **17/17 examples OK** (discovers the new dir, runs `go run`, parses `PASS: editor hash=N`, matches golden). **Honest scope:** the dev-build asset-file-watch trigger (`NewReloadWatcher`) and a real GPU/font UI render are NOT exercised (headless + T-6X04 font-gated) — the hot-reload *reload-registry* and UI *layout* are validated, the deterministic CI-safe halves. Main module `go test ./...` **75 pkgs green** (the example is a separate module, run via `cmd/examplecheck`/go.work); example gofmt + vet + modernize clean; `-race` in CI (CGO absent locally). **App-integration cohort COMPLETE (Groups 1–4 + validation).** **Handoff: `/magic.task`** to promote the 5 held editor specs Draft→Stable.
- 2026-06-03 **`/magic.run` — T-6P04·rem.2 graphDebugSyncSystem + interpreter trace hook (closes Group 4)** — Built the two halves that turn the .1 debug machinery into a live engine↔editor feed. **The dependency-direction puzzle was the crux:** `internal/grapheditor` imports `pkg/visualgraph`, so the interpreter's trace hook must NOT depend on editor/protocol types (that would invert it). Solved with a **neutral DTO + interface defined inside visualgraph**: `ExecutionFrame` (NodeID/NodeType/PinValues/StepIndex — no editor/protocol import) + `TraceRecorder{RecordStep(ExecutionFrame)}`, threaded through a new `Interpreter.RunTraced(g, entry, sink, rec)` while `Run` delegates to `RunTraced(…, nil)` — **purely additive, byte-identical** (the existing DataEval/Deterministic/Branch/StepLimit tests pass unchanged), and the recorder lives **per-pass** in the `run` struct (not on the shared `Interpreter`, which would race across entities); nil rec = zero overhead. `internal/grapheditor/recorder.go` `Recorder` then *implements* `vg.TraceRecorder`, mapping the neutral frame → `editor.GraphExecutionFrame` (stamping wall-clock time) into the DebugStore for one graph+entity — so visualgraph→editor translation stays at the bridge, not in the runtime. `sync.go` `SyncPlugin` (appface.Plugin, **opt-in / editor-attached**, NOT in DefaultPlugins → headless emits nothing) registers `grapheditor.DebugSync` in `PostUpdate`; each frame it `DrainEvents()` and maps each `TraceEvent` to the matching **Engine→Editor** `pkg/protocol` message (Err → `GraphRuntimeError`; breakpointed → `GraphBreakpointHit`; else a single-frame `GraphExecutionTraceEvent`) → an injected `GraphEventSink` (app wires it to the editor transport; tests capture). The drain is **unconditional** (a nil sink still clears the queue so it can't grow); `GraphLiveUpdate` is the *inbound* Editor→Engine direction, owned by the IPC receiver, not this drain — emitting only the three outbound kinds is the honest scope. The per-entity "execute graphs each frame" ECS system is separate Track-P runtime work (not the editor bridge); the hook + sync seam are complete and round-trip-testable end-to-end (RunTraced → DebugStore → DebugSync → captured protocol msgs) for T-6T07. **visualgraph 89.5% / grapheditor 97.6% cov** (RunTraced nil-vs-capture + per-step frames + pulled-input values; recorder frame+error map+timestamp; SyncPlugin build/schedule/name/nil-store; system all-3-msg-kinds + double-drain-empty + nil-sink-still-drains). default `go test ./...` **75 pkgs green**; vet + modernize + `go build -tags editor` clean; own files gofmt-clean (3 pre-existing CRLF files in pkg/visualgraph untouched — out of scope, environmental); `-race` in CI (CGO absent locally). **Group 4 complete (T-6P04·rem.1+.2); visual-graph editor bridge done.** **Handoff: `/magic.run`** T-6T07 (cohort's last task) → then `/magic.task` to promote the 5 held editor specs.
- 2026-06-03 **`/magic.run` — T-6P04·rem.1 grapheditor concrete impls + DebugStore (Group 4, half 1)** — Built the engine-side concrete implementations of the `pkg/editor` visual-graph contract, mapping the headless `pkg/visualgraph` runtime onto the editor DTOs. **Split T-6P04·rem into .1 (this) + .2 (sync system + trace hook)** because the contract impls are a self-contained, fully-testable unit while the IPC-emitting ECS system needs the App schedule + an interpreter instrumentation hook (a different concern with its own integration surface). **Placement call:** new package `internal/grapheditor/` — `pkg/editor` is **guard-tested contract-only** (`TestEditorPkgIsContractOnly`: no func bodies, no `internal/` imports), so concrete impls cannot live there; and the package is **untagged** (not `//go:build editor`) because `graph.go` says impls are "registered only when an editor is **attached**" — that's a *runtime* condition, not build-time, so it joins the default 74-pkg suite. `registryquery.go` `RegistryQuery` (editor.NodeRegistryQuery): translates `vg.NodeDescriptor/PinDescriptor` → editor DTOs (the interesting mapping: `vg.Kind==Execution` → editor's `Execution bool`, `vg.Direction`→`editor.Direction`), List/Search(+category filter)/Get/`GetCompatibleNodes` (a drag from a pin in direction D seeks nodes with an *opposite*-direction data pin whose type is assignable — reimplements `visualgraph.assignable`'s `"" || "any" || ==` rule, unexported there)/`GetTypeHierarchy` (structural `[T, any]`; editor refines with its TypeRegistry). `editorplugin.go` `EditorPlugin` (editor.GraphEditorPlugin): Open/Close → DebugStore; OnNodeSelected merges the static node type (from the graph) with runtime stats (from the store); **OnConnectionChanged reuses the exact per-connection checks ValidateGraph runs** (endpoint exists / Output→Input / kind match / data-type assignable) as **advisory, never-mutating** pre-commit validation — the §4.1 guarantee that an invalid edit is rejected *before* it changes state (removals are always structurally safe); OnPropertyChanged treats an undeclared property as a *warning*, not an error (nodes may accept dynamic props). `debugger.go` `Debugger` (editor.GraphDebugger): the **non-invasive debugging model** — rather than pausing live frames (which would stall the sim loop), the interpreter records an execution trace and the debugger *replays* it: StepOver/StepInto (same until subgraphs exist; trace is flat) advance a cursor, Continue rewinds it, plus breakpoints, variable defaults, and latest-frame pin values. `debug.go` `DebugStore`: the shared state behind both — RWMutex-guarded per-graph breakpoints/exec-count/last-error/latest-frame, a **bounded (256) trace ring** (debug memory can't grow without bound), and a **drainable `TraceEvent` queue** that is deliberately **protocol-free** (carries `editor.GraphExecutionFrame` + graphID/entityID/breakpoint-flag/err) so .2's sync system owns the `pkg/protocol` mapping and this package imports no wire types. **97.4% cov** (registry list/search/category/compatible×3-dirs/hierarchy; plugin open-close-inspect/unknown-node/all-7-connection-rejections/property known-unknown-missing-unloaded; debugger breakpoints/not-open/stepping-exhaust-continue/pin-value/variables; store events+breakpoint-flag/record-error/ring-cap/concurrent). default `go test ./...` **74 pkgs green**; vet + modernize + gofmt clean; `-race` in CI (CGO absent locally). **Handoff: `/magic.run`** T-6P04·rem.2 (graphDebugSyncSystem + interpreter trace hook) → T-6T07.
- 2026-06-03 **`/magic.run` — T-6O05·rem providers + parity (closes Track O + Group 3)** — Added the three remaining providers + the INV-7 parity harness. `provider_anthropic.go`: Messages API ↔ canonical — the wire-shape differences are the interesting part: **system prompt hoisted** to a top-level `system` field (not a role-system message), **required `max_tokens`** (from the canonical `max_output_tokens` param or default 1024), `anthropic-version` header, multi-text-block concat. `provider_gemini.go`: generateContent — `systemInstruction` + `contents`/`parts`, **role mapping** assistant→model (Gemini has no "assistant"), model-in-path `:generateContent`, `usageMetadata` token names. `provider_local.go`: **one line** — `RegisterProvider("local", newOpenAI)` — the L1 §2 insight that local servers (ollama/llama.cpp/vLLM) already speak OpenAI-compatible, so "local" is zero extra wire code with full native SSE, just a different base URL. **DRY refactor (C24 simplicity):** all 4 providers shared one POST+error-map+read flow → extracted `httpclient.go` `postJSON` (status→coded-error INV-5 in one place) and refactored `openAIProvider.Complete` onto it (behaviour-preserving, existing httptest tests stay green); `completeAsStream` adapts the non-SSE providers to the `Stream` contract (one Complete → one final chunk) so **chat works through any provider** while native per-provider SSE stays a refinement. `parity.go` `CheckParity`/`RunParity` (INV-7): a provider's canonical result must be identical in-process vs across the **OOP JSON wire boundary** — `completeOverWire` marshals the request to the wire (→subprocess), runs the provider, marshals the response back (→caller), through the same `pkg/protocol` JSON crossing an OOP plugin uses; a deterministic `FakeProvider` makes the two invocations comparable. **Honest INV-7 scoping:** the OOP loader (T-6N04) drives *lifecycle* phases only, not arbitrary method RPC, so a true spawned-subprocess provider call isn't buildable yet — the harness proves the *canonical-boundary* half (the design claim that only canonical types cross the boundary, mode-invariantly), with full subprocess-RPC parity documented as the follow-up when OOP method dispatch lands. Shared `messageText`/`intParam` helpers consolidated in canonical.go. **89.3% pkg cov (`-tags editor`)** (anthropic build/parse/HTTP/header/stream-fallback; gemini build/parse/role-map/HTTP/stream; local OpenAI-compat select; postJSON 401/header-forward; parity fake/error/wire-roundtrip; all-4-registered). default `go test ./...` **74 pkgs green**; editor build + vet + modernize + gofmt clean (also fixed pre-existing config.go + plugin_test.go gofmt-alignment drift — real, not CRLF). C-005 `-race` in CI (CGO absent locally). **Track O fully complete (T-6O01..05); Group 3 done.** **Handoff: `/magic.run`** T-6P04·rem (Group 4) → T-6T07 validation.
- 2026-06-03 **`/magic.run` — T-6O03·rem SSE streaming + 8-method dispatch + token/cost diagnostics** — Made the aiapi plugin actually answer requests. Three cohesive files. `stream.go`: `decodeSSE` is a **pure** OpenAI-SSE decoder (caller supplies the body → unit-testable without HTTP) — `data: {json}` content deltas → sink, terminating on `[DONE]` / a `finish_reason`-set final chunk / sink error / **ctx cancellation checked between chunks (INV-6, cancellable at any boundary)**; malformed→`CodeParse`, cancel→`CodeCancelled` (INV-5); 1 MiB line cap. `openAIProvider.Stream` wired real (`stream:true`); **additive** `oaiRequest.Stream bool json:",omitempty"` keeps Complete byte-stable (same no-regression property as T-6X03's additive Reload). `dispatch.go`: `Dispatcher` routes the **10 standard `assistant.Method*`** — referencing the assistant constants is the drift-free source of truth (aiapi *implements* the protocol; aiapi→assistant is acyclic + both editor-gated) — via a `methodSpec` table (per-method system prompt + lenient `extractInput`: declared-key → prompt/input/text → join string params, robust to caller naming); enforces the per-provider rate limit (INV-8 → `CodeRateLimited`+retry-after), records usage (INV-9). **Key decoupling:** chat streams through the provider `Stream` forwarding each delta to an **injected `StreamEmitter`** — the app wires that to the ECS event bus (an AssistantStream event readable next frame via T-6X01's swap), so `pkg/plugins/aiapi` never imports the bus (same injection seam as `ContextProvider`/`diag`). `diagnostics.go`: 3 metrics (`aiapi/tokens_input|tokens_output|cost_usd`) behind a `UsageRecorder` interface + a `diagRecorder` whose `Push` is **zero-cost until a reader subscribes** (INV-1 — recording a cost dashboard's worth of usage costs nothing until a dashboard exists). Scoped tight: `generate_*` Command emission (world mutation) + streaming-provider usage deferred; Anthropic/Gemini/local + parity are T-6O05·rem. **89.0% pkg cov (`-tags editor`)** (SSE multi-chunk/[DONE]/keepalive/malformed/cancel/sink/scan-err + Stream httptest ±err; dispatch unknown/not-ready/complete+usage/chat-emitter/rate-limit/provider-err/extractInput; diag register/record/zero-cost-no-reader/skip-zero). default `go test ./...` **74 pkgs green**; editor build + vet + modernize + gofmt clean; C-005 `-race` in CI (CGO absent locally). **Unblocks T-6O05·rem (closes Track O).** **Handoff: `/magic.run`** T-6O05·rem.
- 2026-06-03 **`/magic.run` — T-6O01·rem AI-API plugin lifecycle + ServiceRegistry** — Wired the first-party `pkg/plugins/aiapi` plugin into the engine lifecycle on top of the T-6N03b `appface.FullPlugin` extension. `plugin.go`: `AIAPIPlugin` (New factory + SetConfig + Build/Ready/Finish/Cleanup). **Key design call:** L1 §4.11 wants `Ready() bool` (false until creds resolve) but `appface.FullPlugin.Ready` returns nothing — resolved by tracking readiness **internally** in the `ServiceRegistry.ready` flag: `Ready` attempts `Select(cfg)` + `resolveSecret` and `activate`s the service on success, else logs + stays not-ready (non-blocking graceful degrade, the L1 intent). `service.go`: `ServiceRegistry` realizes the L1 §4.12 service-registry pattern (mirrors `internal/audio.ServiceRegistry`) — an `AIService` interface (Complete/Stream/Ready) published as a World resource so the assistant manager / editor panels reach the active provider through a stable handle and degrade gracefully (`ErrServiceNotReady`) when absent; RWMutex guards activate (Ready) vs Complete/Stream concurrency. `manifest.go`+`plugin.toml`: `//go:embed` self-describing manifest — **single-line arrays** are mandatory for the SDK's minimal stdlib TOML-subset parser (`ParseManifest` splits per line). **Strongest test = dogfooding:** the embedded manifest is run through the real `pkg/plugin.ParseManifest`+`Validate`, proving it's loadable (catches manifest/SDK drift, not string asserts). INV-1 redactor seeded in Ready (exposed via `Redactor()` for the future dispatch log path); INV-4 network.outbound-required asserted. Scoped tight: method dispatch (T-6O03·rem) + extra providers (T-6O05·rem) correctly deferred; `Finish` is a documented placeholder. No `internal/` import — the host app wires the compile-time factory `RegisterFactory(aiapi.PluginID, aiapi.New)`. **88.4% pkg cov (`-tags editor`)** (manifest validate, lifecycle ready/graceful×2/nil-config/cleanup, service not-ready/forward/deactivate); default `go test ./...` **74 pkgs green**; editor build + vet + modernize clean; C-005 `-race` in CI (CGO absent locally). **Unblocks T-6O03·rem.** **Handoff: `/magic.run`** T-6O03·rem.
- 2026-06-02 **`/magic.run` — T-6H02·rem http transport + ContextProvider (Group 3 start)** — Built the two remaining standalone AI-assistant primitives. `pkg/assistant/http.go`: `HTTPTransport` (stdlib `net/http`, C-003 — no dep) + `HTTPConnection` implementing the existing `Connection` contract. **Design call:** HTTP couples request+response in one round-trip, so `Send` performs the POST (`http.NewRequestWithContext` → ctx-bound cancel/timeout, INV-5) and **buffers** the decoded reply; `Receive` returns that buffer single-shot — the same Send-records/Receive-replays split `MemConnection` uses, so the manager's `Dispatch` (Send-then-Receive) works transport-agnostically. 8 MiB response cap (defence-in-depth vs a hostile backend). `pkg/assistant/context.go`: `ContextProvider` + `EditorContext` (the 5 L2 fields) + info types; `GetEditorContext(caps)` is **capability-gated read-side** (INV-2) — ReadScenes→entities+scene+commands, ReadTypeRegistry→types, ReadDiagnostics→diag — and **never invokes an ungated source closure** (asserted in tests). Sources are **injected** func fields (`ContextSources`), the same decoupling pattern as the definition `TypeResolver`, so `pkg/assistant` imports no world/typereg/diag; nil accessor ⇒ zero value (partial wiring safe). L1 §4.9 honoured structurally — only metadata (names/counts/numeric diag) is sourced, never field values / log text / .env, so no secret-leak path even with FileSystemAccess. **91.0% pkg cov (`-tags editor`)** (httptest round-trip + manager-Dispatch integration, non-2xx/bad-JSON/close/single-shot/cancel/transport-error; context full/filtered/diag-only/no-caps/nil-sources). default `go test ./...` **74 pkgs green** (assistant excluded by build tag); `go build -tags editor ./...` + vet + modernize clean. **ws transport** stays behind the ws-dep ADR (user decision). **Unblocks T-6O01·rem.** **Handoff: `/magic.run`** T-6O01·rem.
- 2026-06-02 **`/magic.run` — T-6N04 OOP loader + T-6N03b in-process lifecycle (Group 2 complete)** — Built the compile-time factory registry and full lifecycle driving alongside the out-of-process loader. `pkg/app/appface/lifecycle.go`: `FullPlugin` optional interface (Ready/Finish/Cleanup) checked via type assertion — non-breaking; existing `Build`-only plugins unchanged. `pkg/app/app.go`: `lifecycle []FullPlugin` + `AddPlugin` enrols `FullPlugin`s; `Run()` calls `Ready` after startup, `Finish`/`Cleanup` (`slices.Backward`, reverse order) on exit. `internal/plugin/registry.go`: `RegisterFactory`/`LookupFactory` — compile-time `PluginID → func() appface.Plugin` (Go has no .so/func-by-name). `internal/plugin/inproc.go`: `LoadInProcess` (factory → `Build` → `Ready` → `StateActive`) + `FinishInProcess`. `pkg/protocol/plugin.go`: `PluginInit`/`PluginHello`/`PluginLifecycle`/`PluginLifecycleDone`/`PluginError` + `LifecyclePhase` consts; `codec.go` +5 Decode cases. `internal/plugin/oop/supervisor.go`: `Supervisor` — `spawnFromCmd` (stdin/stdout pipes + `cmd.Dir` cwd-restrict + `handshake`), `DriveLifecycle` (ctx-deadline + `MarkFailed` on any error INV-8), `monitorProcess` goroutine (crash → `MarkFailed`, never panics). `internal/plugin/oop/proxyplugin.go`: `ProxyPlugin` implementing `appface.FullPlugin` — routes each phase to supervisor, failure → `StateFailed` not panic (INV-8), Cleanup → `StateDisabled`. Subprocess tests use `TestMain` + re-exec pattern (test binary acts as fake OOP plugin). **internal/plugin 93.6% / internal/plugin/oop 90.6% cov**; `go test ./...` **74 pkgs green**; build/vet/modernize clean. **Handoff: `/magic.run`** T-6H02·rem (Group 3 start).
- 2026-06-02 **`/magic.run` — T-6N03a plugin discovery + capability-grant approval (Group 2 start)** — Built the "discover and approve" half of the in-process loader on the existing `internal/plugin.Manager`. `discovery.go`: `Source`/`DirSource` walk an `fs.FS` for `plugin.toml` → `ParseManifest` (bad manifests collected, not fatal — a malformed manifest mustn't abort the scan). `grants.go`: `ResolveGrants` folds `RequiredCaps` over their approval tiers (TierGranted auto / TierPrompt decide-once-and-persist / TierAlwaysPrompt prompt-every-session, never persisted — INV-9 no silent escalation) against a `GrantStore` + `PromptFunc`; `MemoryGrantStore` + `MarshalGrants`/`UnmarshalGrants` give deterministic JSON persistence. `install.go`: `DiscoverAndRegister` scans → resolves grants → `Manager.Register` (compat INV-2 + dup INV-5 → `StateApproved`) → `DiscoveryReport`. **Scoping discovery:** split T-6N03·rem because the **lifecycle-driving** half can't be done cleanly yet — Go can't load an in-process plugin from a manifest string (no `.so`, no func-by-name), so it needs a **compile-time factory registry**, and `Ready/Finish/Cleanup` need an `appface.Plugin` lifecycle extension (today `Build` only, a Phase-2 change). That half (T-6N03b) folds into T-6N04 (shared lifecycle). **91.4% pkg cov** (discovery good/bad, all 3 tiers + persist/deny/nil-prompt, JSON round-trip, register approved/deny/incompat/dup); `go test ./...` **73 pkgs green**, build/vet/modernize clean; C-005 `-race` in CI. New files, same package as Manager, no cycle. **Handoff: `/magic.run`** T-6N04 (+ T-6N03b).
- 2026-06-02 **`/magic.run` — T-6A03.2 definition hot-reload system (T-6A03 complete; vertical end-to-end)** — Connected the Group-0 asset-event bridge (T-6X03) to the T-6A03.1 instantiation adapter. `internal/definition/hotreload.go`: `NewHotReloadSystem` (PreUpdate) drains `EventReader[asset.AssetEvent[Definition]]` → on `Modified` re-decodes + `InstanceStore.Reload` (despawn prior + re-instantiate); `DefinitionLoader` (`asset.AssetLoader[def.Definition,struct{}]`) lets definitions ride asset hot-reload; `HotReloadPlugin` wires loader + `WatchAssetType[Definition]` + InstanceStore resource + system. **Key design:** the AssetEvent is a pure *signal* ('path X changed'); the new *value* comes from an **injected `DecodeFunc`** (app→VFS), so the system never races the asset store's async decode and `internal/definition` stays free of asset internals — the same signal/value split that made T-6X03 ECS-decoupled. Alloc-free `IsEmpty` gate (C-004); decode/realization errors slog-logged, never fatal. **96% pkg cov** (full system via `Run`, no-bus no-op, non-Modified skip, decode-error, loader, plugin ±server). *Caveats:* `.json` loader extension collides with the scene loader (pre-existing AssetServer compound-ext limit); Flow action *execution* still deferred (collected in `Instance.Actions`). `go test ./...` **73 pkgs green**, build/vet/modernize clean; C-005 `-race` in CI. **Hot-reload vertical complete (bridge + adapter + system).** **Handoff: `/magic.run`** Group 2 (T-6N03·rem → N04).
- 2026-06-02 **`/magic.run` — T-6A03.1 definition→World instantiation adapter + instance registry** — Execution revealed T-6A03 sat on a whole **unbuilt definition→World pipeline** (only the test `recordingSink` existed — no real `CommandSink`→World adapter, no reflective name+map→component construction), so split T-6A03 into **.1** (this) + **.2** (the hot-reload event-consumer system). `internal/definition/instantiate.go`: `worldSink` realizes `definition.CommandSink` — SpawnEntity→`world.SpawnEmpty`; InsertComponent→ injected `TypeResolver(name)→reflect.Type` + **`encoding/json` round-trip coercion** (the stdlib decoder does the float64→int / map→struct coercion that `typereg.DynamicObject.Set`'s exact-type-match cannot) + `w.Insert` (auto-registers the component); SetParent→`hierarchy.ChildOf`. `Instance{Entities, Actions}` + `InstanceStore{byPath}` with `Despawn`/`Reload` = the per-definition spawned-entity registry hot-reload needs (mirrors `scene.SceneSpawner`). **Design calls:** (1) **inject** the `name→type` resolver → `internal/definition` is registry-agnostic (app wires typereg `ResolveByName`→`reg.Type`; tests inject a map); two resolvers by phase — validation-time `def.TypeResolver` (name exists?) vs realization-time `name→Go type`; (2) Flow **action execution deferred** — actions collected on `Instance.Actions` for a later state-machine pass; instantiation only produces entities (UI/Scene); (3) boundary mismatches (unknown realization type, coercion failure) collected in `errs`, never silent. **97.8% cov** (spawn+components+`ChildOf`, unknown-type + coercion errors, flow-action collection, despawn idempotent, store `Reload` replace, defensive bad-refs); `go test ./...` **73 pkgs green**, build/vet/modernize clean; C-005 `-race` in CI. New package, no cycle. **Unblocks T-6A03.2.** **Handoff: `/magic.run`** T-6A03.2 (needs a `definition` AssetLoader + `WatchAssetType[Definition]`) or T-6N03·rem.
- 2026-06-02 **`/magic.run` — T-6X03 asset-event ECS bridge (Group 0 enabler for hot-reload)** — Built the missing bridge between the asset framework and the ECS event bus, the prerequisite the re-plan surfaced for T-6A03. `pkg/asset/events.go`: `AssetEvent[A]{Created/Modified/Removed; ID; Path}` + `WatchReloads[A](s, emit)` registering a typed emitter **and** a path-keyed dispatcher; `Load` now records a `path→(reflect.Type, AssetID)` map (the **`AssetServer` core extension** the Skeptic flagged — previously only `inflight` path→loading existed); `Reload[A]` fires `AssetEvent[A]{Modified}` synchronously (does not wait on the async decode — consumers key on the stable `Path`); `dispatchReload(path)` resolves a runtime-typed trigger back into the generic `Reload[A]` via the recorded type. `watch_dev.go` `NewReloadWatcher` (//go:build dev) wires the existing stdlib `fileWatcher.onReload`→`dispatchReload`. `internal/asset/bridge.go` `WatchAssetType[A](s, w)` is the ECS bridge: `RegisterEvent` + a `WatchReloads` emit that `Send`s on the world bus (readable next frame via T-6X01's swap). **Design calls:** (1) the typed-event problem (a runtime `reflect.Type` can't instantiate a generic `AssetEvent[A]`) is solved by **per-type pre-registered closures** (`reloadEmitters`/`reloadDispatch` keyed by `reflect.Type`) — the standard Go runtime-generic-dispatch pattern; (2) the event path is **decoupled from ECS** (emit is a plain callback; the bridge supplies the bus-send) so `pkg/asset` stays ECS-free; (3) **additive** — `Reload` only emits when a watcher is registered, so existing callers are unchanged; (4) the event mechanism works in **all** builds, only auto-file-watching is `//go:build dev`. *Known scoping limit:* the dev watcher stats raw fs paths while `loaded` keys on the Load path — the caller must align them (documented); a VFS-aware watcher is a future refinement. **pkg/asset 94.8% / internal/asset 100% cov** (WatchReloads/dispatchReload/Reload/Load all 100%, + a dev mtime→event end-to-end test under `-tags dev`); `go test ./...` **72 pkgs green**, build/modernize clean; C-005 `-race` in CI. **Unblocks T-6A03.** **Handoff: `/magic.run`** T-6A03 (or T-6N03·rem).
- 2026-06-02 **`/magic.task` — App-integration cohort tail re-decomposed (user chose re-plan over piecemeal execution)** — A `/magic.run` found **both** remaining Group 1 tasks **prerequisite-blocked** by work the v1.27.0 decomposition folded silently into the feature tasks; the user (AskUserQuestion) chose **re-plan**. Hoisted two gate tasks into Group 0: **T-6X03** asset-event ECS bridge — `AssetEvent[A]{Created/Modified/Removed}` + an `AssetServer` path→(type,AssetID) map (a *core* extension — today only `inflight` path→loading exists) + a `//go:build dev` plugin wiring the existing stdlib `fileWatcher.onReload`→`Reload`→bus (the asset framework has **no** event mechanism) — gating **T-6A03**; **T-6X04** the `x/image/font` **C-003 first-third-party-dep ADR** gating **T-6D04** + the T-6C04 overlay — explicitly a **user decision**. Split Group 3 into 4 (H02·rem http+ContextProvider [ws behind its own ADR] / O01·rem lifecycle+ServiceRegistry / O03·rem SSE+8-method-dispatch+diagnostics / O05·rem providers+parity ← N04). Found T-6D04's **interaction half is not font-gated** (only the font-atlas upload is) → 2 sub-tasks. C24 Skeptic: T-6X03 touches the asset **core** (not just glue) + T-6A03 hides a `DefinitionInstance`→spawned-entities registry — each ~2 sub-tasks. **Plan-only — 0 promotions** (5 editor-feature specs stay App-integration-blocked Draft); INDEX unchanged (v2.42.0); PLAN/TASKS 1.27.0 → **1.28.0**; RULES parity v1.8.0. **Handoff: `/magic.run`** T-6X03 (or T-6N03·rem, parallel); **T-6X04 + the ws ADR await a user decision.**
- 2026-06-02 **`/magic.run` — App-integration G1: T-6B03 window-sync system** — First consumer of the Group-0 loop primitives. `internal/window/sync.go` `SyncPlugin` + `windowRuntime` resource (backend + last-applied snapshot + `PrimaryWindowRes` + exit policy). The `PreUpdate` `window.Sync` system does three things: **(1) state→backend** — for each `Window` entity, create on first sight, else `DiffWindow(prev,cur)` and `ApplyChanges` only when `HasChanges` (**INV-4**: unchanged ⇒ empty diff ⇒ no backend call), and `DestroyWindow` for despawned entities (clearing the primary marker); **(2) backend→state** — `PollEvents` → mirror each `PlatformEvent` on the bus (readable downstream because T-6X01 swaps it) + fold focus/size/move/scale back into the component **and the snapshot** (so the OS-driven change isn't re-applied next frame); **(3) close→exit** — `CausesAppExit(kind, ExitCondition, isPrimary, remaining)` → `app.RequestExit(w, 0)`. **Design calls:** change-detection comes from the snapshot+diff, **not** the `Changed[T]` filter (still a Phase-1 scaffold that can match-all); `remaining = max(Count-1, 0)` computed *before* an inline despawn so `OnAllClosed` resolves correctly for multiple same-frame closes; one reused `Query1[Window]` + persistent snapshot map ⇒ zero per-frame alloc on the steady path (unchanged `DiffWindow` returns a stack value). **Opt-in** — NOT added to `DefaultPlugins`, so headless CI/servers stay window-free; nil `Backend` ⇒ `HeadlessWindowBackend`. `internal/window`→`pkg/app` is a one-way (acyclic) edge — pkg/app never imports window. Backend errors are slog-logged, never fatal (a windowing failure shouldn't halt the loop). **90.4% pkg cov** (sync.go ~100%, all 3 exit policies + fold + error paths + a full-App close-stops-loop test); `go test ./...` **71 pkgs green**, build/modernize clean; C-005 `-race` unavailable (CGO absent). **Handoff: `/magic.run`** G1 remainder (T-6A03 needs an asset-event primitive first; T-6D04 ADR-gated) → G2 T-6N04.
- 2026-06-02 **`/magic.run` — App-loop foundation: T-6X02 `AppExit` loop primitive (closes Group 0)** — Built the system-facing exit path on top of the now-live event loop (T-6X01). The gap: `App.Exit()` mutates the `*App`, but a *system* only ever receives `*world.World` — it had no way to stop the loop, which blocks T-6B03's window-close → exit. `pkg/app/exit.go`: **`AppExit{Code uint8}`** event + **`RequestExit(w, code)`** (sends on the bus; **no-op off-loop** so systems stay usable in a bare-World test); the runner **drains at the tail of `update()`** into the existing `shouldExit` flag (so both the default and any custom runner honor it), and a non-zero `Code` surfaces as **`*ExitError`** from `App.Run` (process-status parity, Bevy-aligned). Added **`App.ShouldExit()`** so custom runners can loop on it. **Design calls:** (1) **event, not resource** — a system already has the event bus as its one channel into the loop, and T-6X01 made it deliver same-frame (the reader reads the *current* write buffer when `cursor < sentCount`, so an exit raised in `Update` is caught at that frame's tail — no 1-frame lag); (2) **C-004** — `EventReader.All()` allocates a closure, so the drain gates on the alloc-free `IsEmpty()` → **zero alloc in steady state**, the closure builds only on the shutdown frame; (3) bus **registered in `NewApp`** so `RequestExit` always works on an App's World regardless of Run/Update path. No cycle: `pkg/app` → `internal/ecs/event` (one-way). **87.6% pkg cov** (exit.go 100% — IsError/RequestExit/Error/drain/exitErr); `go test ./...` **71 pkgs green**, build/modernize clean; C-005 `-race` unavailable (CGO absent). **This unblocks T-6B03:** `internal/window` → `pkg/app` is acyclic, so the window-sync system can map a `CausesAppExit` platform event to `RequestExit`. **Handoff: `/magic.run`** T-6B03 window-sync.
- 2026-06-02 **`/magic.run` — App-loop foundation: T-6X01 event-bus loop integration (`EventsPlugin`)** — While scoping T-6B03 window-sync I found the **ECS event bus is not wired into the App loop**: `SwapAll` is called only in a manual example, never per-frame, so the double-buffered buses never rotate — every system's `Send` accumulates in the current buffer and is never promoted to where readers observe it. Added `internal/ecs/event/EventsPlugin`: a `First`-schedule `ecs.SwapEvents` system running `SwapAll(w)`+`CleanupAll(w)`, placed first in `DefaultPlugins` so the swap precedes any First reader. This is the **engine-loop half of the two-frame delivery contract** and a shared prerequisite — it unblocks window `PlatformEvent` emission (T-6B03), AppExit-as-event, and ai SSE/event streaming (O03). No cycle: `internal/ecs/event` → `appface` (not `pkg/app`); `pkg/app` → `internal/ecs/event` is one-way. Tested via a capture `appface.Builder`: 2-frame retention then clear (send → swap [Len 1, retained] → swap [Len 0, cleared]) + empty-registry no-op. `go test ./...` **71 pkgs green**, build/modernize clean. **Process note:** picked this over T-6B03 because window-sync's event emission is *inert* without it — App-integration surfaces missing loop primitives bottom-up (T-6C04 needed only resources; T-6B03 needs events → needs the swap; exit needs AppExit [T-6X02, still missing]). **Handoff: `/magic.run`** T-6X02 AppExit → T-6B03 window-sync.
- 2026-06-02 **`/magic.run` — App-integration Group 1 begun: T-6C04 diagnostic built-in metrics (built-ins Done; overlay deferred)** — First App-integration deliverable, establishing the **`Plugin`+resource+system pattern** for the whole cohort. `internal/diag/builtins.go` `DiagnosticsPlugin` (`appface.Plugin`): at Build it registers `engine/{fps,frame_time_ms,entity_count}` diagnostics, `SetResource`s the `*DiagnosticsStore`, and adds a `Last`-schedule `diag.BuiltinMetrics` system. `recordFrame` reads `gametime.RealTime.DeltaSeconds()` (wall-clock, set in the `First` schedule) → frame-time + FPS (skipped on zero delta), and `liveEntityCount` sums archetype rows; all gated by `HasAnyReader` so collection is **zero-cost until a consumer (overlay/test) registers a reader** (INV-1). **96.7% cov** — `recordFrame` (timing + zero-delta + gate), `collectBuiltins` (entity-count), and `Build` (via a fake `appface.Builder` recording systems/resources — avoids a pkg/app import cycle). **Scoping/process decisions:** (1) picked T-6C04 over the canonical first-task T-6B03 because window-sync needs an `AppExit` mechanism that **doesn't exist in pkg/app** + query/event-bus wiring — a larger, dependency-incomplete bite; T-6C04 (resources + Time + store) is genuinely self-contained. (2) The **debug overlay is deferred** (renders via UI text → Track D / x/image/font ADR) — built-in *metrics* are the shippable half. `go test ./...` **71 pkgs green**, build/modernize clean; C-005 `-race` unavailable (CGO absent). Reuses gametime + diag + world (all Stable), zero new deps. **Handoff: `/magic.run`** Group 1 cont. (T-6A03 hot-reload, T-6B03 window-sync [needs an AppExit primitive first]).
- 2026-06-01 **`/magic.task` — P6 App-integration cohort decomposed (plan-only; no promotions)** — Turned the paused App-integration milestone into an activatable, dependency-ordered plan in `tasks/phase-6.md` (new "App-Integration Cohort" section): 4 groups + validation. **G1 engine-core glue** (T-6A03 definition hot-reload, T-6B03 window-sync, T-6C04 diagnostic built-ins+overlay, T-6D04 ui interaction/font-upload) — independent, each ← its Stable subsystem + the App schedule. **G2 plugin-dist** (T-6N03·rem discovery/lifecycle, T-6N04 OOP loader ← H transports). **G3 ai assistant/API** (T-6H02·rem ws/http+ContextProvider, T-6O01/O03·rem lifecycle+SSE+8-method-dispatch ← N + event bus). **G4 visual-graph** (T-6P04·rem concrete impls + `graphDebugSyncSystem` ← schedule + protocol IPC). **T-6T07** end-to-end validation. Critical path `{G1 ‖ N03·rem} → N04 → G3 → P04·rem → T-6T07`. **Planning-Skeptic flags:** G3 under-sized (→3–4 tasks), the App-schedule/plugin-lifecycle is the shared bottleneck (serialize schedule-touching tasks), and two C-003 ADR gates (x/image/font for T-6D04, websocket for T-6H02·rem) precede their tasks. Pre-Planning Stabilization promoted **0** (editor-feature specs are App-integration-blocked). Tactical decomposition only — no spec/INDEX change (v2.42.0); PLAN/TASKS v1.27.0. **Cohort PAUSED — not active**; `/magic.run` activates G1 when feature work resumes. **Handoff: `/magic.run`** (G1) when ready.
- 2026-06-01 **Milestone: P6 standalone work LOCKED; App-integration phase paused for deliberate planning (user decision)** — Assessed at the natural P6 juncture: every cleanly-completable standalone P6 deliverable is done — **tooling E/F/I/J/L/M** (CLI, releasenotes/CI, examplecheck, pkg/version+apidiff, benchcompare, codegen-query), **asset follow-ons** (glTF loader + `.scene.json` decode), **governance promoted** (compat-policy + examples-framework → Stable, 76 total), and the **editor-bridge contract** (P04 `pkg/editor/graph.go` + `pkg/protocol/graph.go`, guard-green). The remainder is a **cohesive App-integration phase** (concrete editor-plugin impls + `graphDebugSyncSystem`; N04 OOP loader; O03/H02 SSE+dispatch; engine-core ECS-system glue) — cross-cutting work best **decomposed via `/magic.task` or ventilated via `/magic.analyze`** rather than improvised piecemeal or pursued via the marginal T-6M02. Held editor-feature specs (cli/plugin-distribution/ai-assistant/ai-api/visual-graph-bridge) promote (or narrow) once their App-wiring lands. Also recorded a **benign repo-wide finding**: the working tree is CRLF (Windows checkout, ≥Phase 1 — 188 CR in `internal/ecs/world/world.go`); `gofmt -l` flags 284 files purely on line-endings (CR-stripped → `gofmt -d` empty), git autocrlf normalizes to LF on commit, code is gofmt-canonical in content — no action taken (mass-conversion would fight the IDE/git for zero correctness gain). Build + 71 pkgs green. **Handoff: `/magic.task`** to plan the App-integration phase.
- 2026-06-01 **`/magic.run` — `.scene.json` decode loader (graduates the asset-formats Planned item)** — Implemented `pkg/asset/formats/scene` (`SceneJSONLoader` + `Decode` + `RegisterAll`): decodes `.scene.json` → the portable `scene.SerializedScene` by reusing the Phase 3 `scene.UnmarshalJSON` and **adding interning-index range validation the JSON codec lacks** (the binary codec validates indices, JSON didn't — the loader enforces it at the boundary so a malformed scene errors rather than yielding a corrupt asset, INV-2). **Two reality reconciliations vs the spec:** (1) the loader yields `SerializedScene` (the on-disk portable interned form), *not* `DynamicScene` (the in-memory reflection form hydrated from a World) — decode is the loader's half, spawn (`scene` spawn, needs a `TypeRegistry`) is deferred to App integration; (2) `AssetServer.find` keys on `path.Ext` (→ `.json` for `*.scene.json`), so compound-extension auto-routing is a deferred asset-system enhancement (loader used via explicit `Load`/`Decode` until then). **96.2% cov** (round-trip byte-stable golden + malformed-JSON + out-of-range-index cases). Reuses the scene codec → zero new deps (C-003). asset-formats specs graduated `.scene.json` Planned → Implemented(decode): `l1-asset-formats` 0.2.0 → 0.3.0 + `l2-asset-formats-go` 0.2.0 → 0.3.0 (registry sync, no status change — already Stable); INDEX v2.41.0 → v2.42.0; PLAN/TASKS v1.26.0. `go test ./...` **71 pkgs green**, build/modernize clean. **Handoff: `/magic.run`** engine-side editor wiring (the remaining work is App-integration) / T-6M02.
- 2026-06-01 **`/magic.task` — Pre-Planning Stabilization: 2 governance specs Draft → Stable (user-scoped to governance, Option A)** — Assessed all P6 Draft specs against implementation. Promoted the two that are *faithfully realized* with no deferred headline: **`l1-compatibility-policy`** 0.3.0 → 0.4.0 (Track J — `pkg/version` IS the SemVer + Go-toolchain matrix, `MinGoToolchain` matching §3's v0.1.x→Go 1.26.3; `cmd/apidiff` IS the §5.1 breaking-change gate; CI enforces §3/§6) and **`l1-examples-framework`** 0.4.0 → 0.5.0 (Track I — `cmd/examplecheck` is the §4.5 mechanical-validation gate, `_template`/README the §4.4 contract, `goldens.json` the committed gate; `cmd/showcase/` is deferred *by the spec itself* §4.6, so not an overclaim). Canonical References populated on both. Both are **L1-only governance** (no L2 children) → layer-clean; **C24 Skeptic clean** (no Stable spec hard-depends on them; Related links are soft → no broken Stable→Draft edge). **Held Draft (overclaim avoidance, asset-formats lesson):** the editor-feature specs (cli-tooling, plugin-distribution, ai-assistant-system, ai-api-plugin, visual-graph-editor-bridge) are *core-done with substantial deferred App-integration surface* (OOP loader, ws/http transports, SSE, full CLI subcommands, graphDebugSyncSystem) — promoting would claim more than is built. Their path to Stable is either App-integration wiring or narrow-then-promote (asset-formats Option A) — a future decision. INDEX v2.40.0 → v2.41.0 (**Stable 74 → 76, Draft 38 → 36**); PLAN/TASKS v1.25.0. RULES parity v1.8.0. **Handoff: `/magic.run`** scene.json / engine-side editor wiring.
- 2026-06-01 **`/magic.run` — Track J02 api-diff: `cmd/apidiff` (Done; Track J complete)** — Built the API-stability gate completing Track J. `cmd/apidiff` snapshots the exported surface of `pkg/...` via `go/ast`+`go/printer` (stdlib-only, C-003 — avoids the `golang.org/x/tools/cmd/apidiff` third-party dep): `extract.go` (`Symbol` = name/kind/signature; exported func/method/type/const/var; excludes unexported names + methods on unexported receivers; handles generic receivers + type aliases; signatures **printer-rendered + whitespace-normalized** so reformatting is never a spurious diff), `snapshot.go` (`Snapshot` JSON + `scanPackages` walk, skips `_`/`.`/testdata/_test.go), `diff.go` (`Diff` matches by kind+name → **removed/changed = breaking**, added = minor), `main.go` (`-pkgs`/`-snapshot`/`-update`/`-json`, exit 1 on breaking). **85.5% cov**; dogfooded — `api/snapshot.json` captures **35 packages**, self-compare clean. The fourth "committed snapshot → drift" member: `pkg/version` defines what *breaking means* (caret SemVer), apidiff *detects* it in the real source. Bugfix during impl: `go/printer` renders `*ast.FuncType` without the name (it lives on `FuncDecl`), so the signature is assembled as `"func "+name+trimPrefix(render,"func")` — required regenerating the snapshot after the fix. `go test ./...` **70 pkgs green**, build/modernize clean. Path reconciled `scripts/api-diff/` → `cmd/apidiff`. **Track J fully complete (J01 pkg/version + J02 apidiff); all P6 tooling tracks done.** **Handoff: `/magic.run`** scene.json loader / engine-side editor wiring, or **`/magic.task`** to weigh P6 promotions.
- 2026-05-31 **`/magic.run` — Track I examples framework: `cmd/examplecheck` (T-6I01/I02 Done) — completes all P6 tooling** — Built a committed-golden gate for the examples: `cmd/examplecheck` discovers `examples/*/main.go`, runs each, parses the `PASS:/FAIL/hash=` convention, and compares **hash-emitting** examples against `examples/goldens.json` (drift → exit 1) while **smoke** examples (multiple `PASS` lines, no single hash) are run-only. Key design: the exec boundary is an injected `exampleRunner func(name)(string,error)`, so `evaluate`/`classify`/compare are unit-tested with canned outputs (no subprocess) — `runExample` (`go run`) + `gitChangedPaths` stay thin integration. `-update` rewrites goldens; `-list` + `-changed <ref>` (git-diff → pure `examplesFromPaths`) drive selective CI builds (T-6I02). Added `examples/_template/` scaffold (the `_` prefix → skipped by both `go ./...` and `examplecheck`) + rewrote `examples/README.md` (conventions + golden gate). **87.2% cov**; **dogfooded** — ran all 16 examples via `-update` → `goldens.json` with 9 hashes (2d/animation/audio/config/diagnostic/gltf/tweening/ui/window; gltf hash matched the earlier glTF run — cross-checked), 7 smoke, 0 fail. `go test ./...` **69 pkgs green**, build/modernize clean. Rationale: the committed golden catches *deterministic behaviour drift* the per-example ×20 stability tests can't (they prove stability, not the value). Path reconciled `scripts/examples/` → `cmd/examplecheck` (testable Go tool). **All P6 tooling tracks E/F/I/J/L/M now done.** **Handoff: `/magic.run`** editor-bridge wiring (App integration) or `/magic.task` to assess P6 status.
- 2026-05-31 **`/magic.run` — Track E build tooling: CI workflow + `cmd/releasenotes` (T-6E01/E02/E03 Done)** — **CI (`.github/workflows/ci.yml`):** `test` (build+vet+`-coverprofile` over ubuntu/macos/windows matrix, Go pinned via `GO_VERSION` env tracking `pkg/version.MinGoToolchain`), `race` (`go test -race ./...` on ubuntu — **first place C28/C-005 actually runs**, since hosted runners have CGO/gcc absent locally), `lint` (golangci-lint-action), `bench` (T-6E02 — pipes `go test -bench -benchmem ./...` through `cmd/benchcompare`; `continue-on-error` advisory because the seed `baseline.json` ns/op is dev-machine-specific while allocs/B are the portable signal). **`cmd/releasenotes` (T-6E03, Go — path reconciled from `scripts/release/` for testability):** `parse.go` (spec title/version/status + Document History table parser; em-dash/separator rows skipped, pipes-in-desc preserved, missing-H1 → filename fallback), `report.go` (`ReleaseNotes` dated changelog + `BreakingChanges` **dogfooding `pkg/version`** — a transition is breaking iff `^prev` doesn't match latest, the same caret rule as plugin compat, so 0.x minor counts), `main.go` (`-specs`/`-since`/`-breaking`/`-o`). **92.1% cov**; dogfooded on the real `.design/main/specifications` corpus. `go test ./...` **67 pkgs green**, build/modernize clean. Anti-hallucination: bench-gate is a CI job step over the tested Go tool, not an ad-hoc shell runner. **Track E core complete** — P6 tooling now F+J+L+E done; remaining: M (codegen), I (examples framework), editor-bridge wiring. **Handoff: `/magic.run`** — Track M / Track I / editor-bridge.
- 2026-05-31 **`/magic.run` — Track J compat: `pkg/version` SemVer kernel extracted (T-6J01 Done, T-6J02 deferred)** — Created stdlib-only **`pkg/version`** as the single source of truth for SemVer: `version.go` (`Version` + `Parse` [v-prefix/pre-release-strip, partial→0] + `Compare` + `String`), `constraint.go` (`Constraint` + `ParseConstraint` Cargo-subset grammar — caret/tilde/`>=`/`>`/`<=`/`<`/`=`/bare/comma-range, caret-on-0.x pins minor — + `Matches` + `IsAny`), `policy.go` (documented engine SemVer policy + `MinGoToolchain` = "1.26.3" from go.mod + `GoToolchainConstraint`/`IsGoToolchainSupported`, dogfooding the constraint evaluator). **DRY extraction:** `pkg/plugin/{semver,constraint}.go` (Track N) refactored from ~190 lines of duplicated logic to **type-alias re-exports** (`type Version = version.Version`, `type Constraint = version.Constraint`, wrapper `ParseVersion`/`ParseConstraint`, `var ErrInvalidVersion = version.ErrInvalidVersion`) — alias `=` preserves type identity so the SDK public API + all Track N tests pass unchanged (verified via `errors.Is` on the shared sentinels). **pkg/version 98.9% cov**; pkg/plugin 86.1% (logic moved out, still >80%), internal/plugin 93.0%, cmd/cli 85.0% all green; `go test ./...` **65 pkgs**, `go build`/modernize clean; C-005 `-race` unavailable (CGO absent). T-6J02 (`scripts/api-diff/` public-surface snapshot + breaking-change CI gate) deferred — it's a CI/tooling deliverable. **Handoff: `/magic.run`** — Track M codegen / editor-bridge / tooling E/I/L.
- 2026-05-31 **`/magic.task` — asset-formats narrowed + promoted Draft → Stable (Option A)** — User chose to scope the spec to the implemented surface rather than build scene.json first. Narrowed `l1-asset-formats` (0.1.0→0.2.0) + `l2-asset-formats-go` (0.1.0→0.2.0): the **Stable surface** is the glTF 2.0 loader (value-based fan-out of scene/mesh/material/texture + deterministic `GltfAssetLabel`, INV-2/INV-4; 90.0% cov + fuzz + `examples/gltf` ×20) + stdlib PNG/JPEG + PCM WAV. HDR/DDS/KTX2/BMP/WebP/TGA/OGG/FLAC/MP3/AAC/fonts/`.scene.json` + glTF animations/skins/morph/KHR are explicitly marked **Planned** (deferred future loaders) so the spec no longer overclaims. **Canonical References populated** on both (gltf label/schema/convert/loader + test, image, audio, example). L2 corrected to the shipped value-fan-out (handle-based `GltfAsset` needs a `LoadContext` the real `AssetLoader.Load(r,settings)` lacks → App-integration); INV-1 cross-package conflict-detection + INV-3 build-tag optional formats noted as planned hardening. **Pre-Planning Stabilization: 2 promoted, 0 other newly eligible.** Also reconciled the stale Phase 5 PLAN checklist (all 10 P5 specs were Stable in INDEX but shown `[ ] Draft [Bootstrap]`) → `[x] Stable`; Phase 5 header `Active` → `Done`. **C24 Skeptic clean:** asset-formats is a downstream consumer (mesh/material/scene/audio) — no Stable spec `Implements` it, so no broken Stable→Draft edge; layer-clean (L1 promoted before L2). INDEX v2.38.0 → v2.39.0 (Stable 72 → 74, Draft 40 → 38); PLAN/TASKS v1.23.0. **Handoff: `/magic.run`** — P6 tooling (Track M/J) / editor-bridge / tooling E/I/L; optional scene.json follow-on.
- 2026-05-31 **`/magic.run` — glTF loader core (T-5B03 partial; clears most of the asset-formats hold)** — Implemented `pkg/asset/formats/gltf/` (stdlib-only, dep-free C-003): `GltfKind`/`GltfAssetLabel` deterministic `(Kind,Index)` addressing (INV-4); glTF 2.0 JSON subset schema + OpenGL component/mode constants; buffer resolution (base64 data-URI + GLB BIN chunk) + accessor de-interleave with exhaustive bounds-checking; **one `mesh.Mesh` per glTF primitive in declaration order** (POSITION/NORMAL/UV0/UV1/TANGENT/COLOR + u8→u16 widening / u16 / u32 indices + topology map); PBR `material.Material` (base-color/metallic/roughness/emissive factors + alpha-mode + double-sided); embedded data-URI/bufferView textures → `renderimage.Image` (reuses `renderimage.DecodeImage`, no duplication); `GltfLoader` `AssetLoader[GltfAsset,LoadSettings]` (.gltf/.glb auto-detect) + `Decode` with panic-recover so malformed input is always a wrapped error, never partial (INV-2) + `GltfAsset.Get`/`Labels`/`MeshMaterial` + `RegisterAll`. **90.0% cov** + `FuzzDecode` (114k execs, 0 panics); `examples/gltf/` fan-out hash-stable ×20 (C29 golden). `go build ./...`/`go vet`/`modernize` clean; `go test ./...` **64 pkgs green**; C-005 `-race` unavailable (CGO absent). Architectural note: the L2 spec's handle-based `GltfAsset` (sub-asset registration via `LoadContext`) can't be honored by the real `AssetLoader.Load(r,settings)` signature — implemented a value-holding fan-out (slices addressed by stable label) which proves INV-2/INV-4 standalone; handle registration stays an App concern. Deferred: `scene.json` loader (`pkg/asset/formats/scene/`), animations/skins/morph-targets (accessor matrix/quat decode + `animation.AnimationClip` samplers), KHR extensions, external-URI resolution. **Handoff: `/magic.task`** to weigh `asset-formats` Draft → Stable (residual gate: scene.json round-trip from the spec's testing strategy).
- 2026-05-31 **`/magic.run` — Track F CLI Tooling (T-6F01/02 Done, F03 partial)** — Replaced the `cmd/cli` Phase-0 stub with a working CLI: stdlib-`flag` `Router` + `Command` interface + `Output` (text vs `--json` stable output INV-4) + global flags (`--json`/`--force`/`--dry-run`) + no-arg structured help (INV-2) + gated command registry (only implemented commands listed, INV-3). Commands: `doctor` (env/version, --json), overwrite-safe `scaffold <component|system|plugin>` (skips existing unless `--force`, `--dry-run` previews, INV-1), `plugin validate <path>` + `plugin list <dir>` consuming the `pkg/plugin` SDK (`ParseManifest`+`Validate`+`Mode`). This makes the Track N plugin SDK usable end-to-end. **85.0% cov**; `go build ./...` clean, `go test ./...` 62 pkgs green, modernize clean. Deferred: `new project` template tree, `asset import|list|build`, full `plugin install/enable/disable/info/remove/doctor` (need templates + asset pipeline + plugin install flow). **Handoff: `/magic.run`** glTF / codegen.
- 2026-05-31 **`/magic.run` — Track P Visual Graph System core (T-6P01/02/03 Done, P04 deferred)** — Implemented `pkg/visualgraph` (runtime, not editor-gated): graph data model (`GraphDefinition`/`Node`/`Pin` with Direction+Kind/`Connection`/`VariableDecl` + lookup helpers), `NodeRegistry` + `NodeDescriptor`/`PinDescriptor` (Register/Get/List/ListByCategory/Search, deterministic sort), `ValidateGraph` (endpoint/direction/kind checks + type-compat INV-3 + 3-colour DFS data-dependency cycle detection INV-4), `Interpreter` (execution-pin chain following + lazy memoized data-input pull, node-step-limit INV-2, `CommandSink` for all mutations INV-1, deterministic given same graph+sink INV-4) + a representative built-in node set (event.OnUpdate, math.Add, logic.Greater, flow.Branch, action.Log/SetVariable covering every execution category). **89.3% cov**; `go build ./...` clean, `go test ./...` 61 pkgs green, modernize clean; C-005 `-race` unavailable (CGO absent). Deferred: editor gateway (`pkg/editor/graph.go` + `pkg/protocol/graph.go`, the l2-visual-graph-editor-bridge contract), TypeRegistry auto-generation of component/event/state nodes, SubGraph/Query node categories, the `"graph"` definition-loader wiring — all need App/editor integration. **Handoff: `/magic.run`** tooling tracks / glTF.
- 2026-05-31 **`/magic.run` — Track O AI API Plugin core (T-6O02/04/05 Done, O01/03 partial)** — Implemented `pkg/plugins/aiapi` (`//go:build editor`): canonical request/response/message/usage types, `Provider` interface + compile-time registry + `Select` (extension point), OpenAI-compatible `provider_openai` with pure `buildOpenAIRequest`/`parseOpenAIResponse` + full `Complete` validated via `httptest` (success + 500→`CodeHTTP5xx`), `Config`/`DefaultConfig`, per-provider `rateLimiter` token bucket (RPM+TPM, injectable clock, INV-8), `E-PLUGIN-AIAPI-{NNN}` codes + `MapHTTPStatus` (401/429/4xx/5xx, INV-5), env credential source + `errs.Redactor` reuse + `crypto`-style secret zeroing (INV-1, keyring/age ADR-gated), `FakeProvider` deterministic responder (INV-7 foundation). **82.3% cov (`-tags editor`)**; default `go build`/`go test ./...` 60 pkgs green (editor excluded); `go build -tags editor ./...` compiles the whole editor surface; modernize clean (string-concat → strings.Builder via -fix). Reuses `pkg/errs.Redactor` cross-package (no duplication). Deferred (App/event/diag/OOP wiring): SSE streaming, 8-method dispatch, plugin lifecycle, diagnostics/cost events, full in/out-of-process parity harness, Anthropic/Gemini/local providers. **Handoff: `/magic.run`** Track P / tooling / glTF.
- 2026-05-31 **`/magic.run` — Track H AI Assistant System (T-6H01/03 Done, H02 partial)** — Implemented `pkg/assistant` (`//go:build editor` — first build-tag-gated editor package, establishing the pattern): `AgentMessage` MCP-shaped JSON protocol + Encode/Decode, `Capability` bitfield (Read/Write/Advanced) + String, standard-method names + required-capability table, `Transport`/`Connection` interfaces + `StdioConnection` (newline-delimited JSON) + `MemConnection` (deterministic test responder), `AssistantManager.Dispatch` (INV-2 capability gate with logged denials, INV-5 `context.WithTimeout`/cancel so a dead agent never blocks the editor, request log) + `ApplyModifications` agent+request tagging for one-undo-group edits (INV-1/4). **88.0% cov under `-tags editor`**; default `go build ./...`/`go test ./...` 60 pkgs green (assistant excluded by build tag — verified the editor gate doesn't affect shipped builds); modernize clean (via `GOFLAGS=-tags=editor`); C-005 `-race` unavailable (CGO absent). Deferred: websocket/http transports (ws dep ADR), `ContextProvider` EditorContext assembly, editor UI panels (editor-repo). Unblocks Track O (ai-api-plugin reuses these transports for OOP + implements the assistant methods). **Handoff: `/magic.run` Track O.**
- 2026-05-31 **`/magic.run` — Track N plugin-distribution SDK core (T-6N01/02 Done, N03 partial)** — Implemented the editor-layer SDK root: `pkg/plugin` (`Plugin`/`PluginGroup` re-export from app, `PluginID`/`Mode`/`State` enums, `Version` + `Constraint` Cargo-subset SemVer with caret-on-0.x semantics — INV-2, `Manifest` + stdlib-only TOML-subset `ParseManifest` + `Validate` — INV-1, `Capability` vocabulary + `CapabilitySet` with tiers + wildcard match — INV-3, `PluginContext`/`CommandIssuer`, `E-PLUGIN` sentinels + `CapabilityError`; **no Go `plugin` .so** per L1 §2) + `internal/plugin/Manager` (registry, INV-2 compatibility resolution, INV-5 duplicate-ID guard, lifecycle states) + `capabilityProxy` (INV-3 runtime enforcement, logged denials) + plugin-ID-tagged commands (INV-7). **pkg/plugin 88.5%, internal/plugin 93.0% cov**; `go build ./...` clean, `go test ./...` 60 pkgs green, modernize clean; C-005 `-race` unavailable (CGO absent). Deferred to App integration: discovery (4 sources), capability-prompt persistence, in-process Build/Ready/Finish wiring, and T-6N04 OOP loader (subprocess spawn + `pkg/protocol` lifecycle, needs Track H transports). Unblocks Track O (ai-api-plugin) + Track F (cli plugin subcommands) + Track M (plugin scaffold). **Handoff: `/magic.run`** for Track O/H.
- 2026-05-31 **`/magic.task` — P6 editor-layer L2 tier mapped (registry sync)** — Placed the 5 new editor-layer L2 specs into PLAN.md Phase 6 under their L1 parents: `l2-{plugin-distribution,ai-assistant-system,ai-api-plugin,cli-tooling,definition-integration}-go` (all `[L2] [Bootstrap]`). Resolves Pre-flight ORPHANED_SPEC × 5 + SYNC_GAP (PLAN was on INDEX v2.37.0, registry at v2.38.0). Annotated `phase-6.md` Track Overview A/F/H/N/O with their L2 refs. Pre-Planning Stabilization: **0 promoted** — the 5 L2s are layer-blocked (Draft L1 parents); editor-layer L1s are Draft concept specs with no impl/examples (C29). 5 hard L2→L1 edges (37 total), acyclic. **Full P6 L1+L2 parity achieved — spec surface complete** (governance L1s compatibility-policy/examples-framework need no L2). PLAN/TASKS v1.22.0, INDEX unchanged (v2.38.0). **Next:** implementation phase — `/magic.run` tooling or editor tracks, or glTF for asset-formats.
- 2026-05-30 **`/magic.spec` — Phase 6 editor-layer L2 tier authored (5 contracts)** — Created `l2-plugin-distribution-go` (public `pkg/plugin` SDK no-`internal/`, `plugin.toml` manifest + SemVer Constraint, `CapabilitySet` + runtime `capabilityProxy`, in-process + OOP loaders sharing one lifecycle, OOP failure isolation over `pkg/protocol`, plugin-ID-tagged commands — INV-1..9; no Go `plugin` .so), `l2-ai-assistant-system-go` (`//go:build editor`: `AssistantManager` + JSON `AgentMessage` over stdio/ws/http, `Capability` bitfield, Command-pipeline tagged undo, worker-pool async cancel/timeout), `l2-ai-api-plugin-go` (`pkg/plugins/aiapi` Provider+canonical types, env/keyring/age credentials with zeroing+redaction, rate limiter, `E-PLUGIN-AIAPI` codes, FakeProvider mode-parity), `l2-cli-tooling-go` (stdlib-flag Router + gated Command registry, overwrite-safe scaffold, `--json`, `ecs plugin` subcommands), `l2-definition-integration-go` (stable `pkg/editor` `DefinitionEditorInterface` + order-based serialization + entity hooks). All `Draft` (L1 parents Draft → layer constraint). Unblocked by multi-repo Stable. 5 new hard L2→L1 edges (37 total), acyclic; dependency order `{plugin-dist ‖ ai-assistant} → ai-api-plugin`, `cli → plugin-dist`, `definition-integration → definition`. **Completes P6 editor-layer L1+L2 parity.** INDEX v2.37.0 → v2.38.0 (112 specs, Stable 72/Draft 40). Post-Update Review (Critic): Invariant Compliance maps each L1 INV to a concrete Go mechanism, contracts only (no impl code), `Implements`/`Related` links valid, C-003 honored (TOML/age/keyring/WS deps explicitly ADR-gated, not silently assumed). **Handoff: `/magic.task`** to map into Phase 6.
- 2026-05-30 **`/magic.task` — P6 engine-core cohort stabilized (12 specs Draft → Stable)** — Pre-Planning Stabilization promoted **6 L1 + 6 L2** (error-core, platform-system, window-system, diagnostic-system, ui-system, definition-system) after re-verifying the C29 P6 gate (`examples/{config,window,diagnostic,ui}` hash-stable ×20 + `go test ./...` 58 pkgs green). All 6 L2 specs' **Canonical References populated** with on-disk-verified source + test + example files. C24 Skeptic cleared: every cohort `Implements` parent is now Stable, and the intra-cohort edges (diagnostic→error-core, definition→ui) are both Stable → no broken Stable→Draft edge; `asset-formats` stays held Draft (nothing in the cohort depends on it). Deferred App-integration glue (window sync, diagnostic overlay/built-ins, definition hot-reload, ui PreUpdate) + the x/image/font ADR are non-blocking (analogous to render-core's nopBackend). INDEX v2.36.0 → v2.37.0 (Stable 60 → 72, Draft 47 → 35), PLAN/TASKS v1.21.0. **Phase 6 cohort = 12 specs Stable.** **Handoff: `/magic.spec`** for the editor-layer L2 tier.
- 2026-05-30 **`/magic.run` — Phase 6 engine-core cohort COMPLETE (17/17, C29 gate closed)** — Run 3 (top layer + validation). **Track D ui** → `pkg/ui` (Style+Val union, Node/LayoutRect/ZIndex, widgets, Interaction/MouseFilter/Focused) + `internal/ui` (single-line flexbox `Solve` with grow/shrink/justify/align/padding/gap/reverse/nested, `SolveIfDirty` dirty-gate INV-1, `FontAtlas` over render-core `DynamicAtlas` INV-4, `HitTest`+`InteractionFor` INV-3, `UiFeature`+`SortByZ` INV-2); **pkg/ui 95.5%, internal/ui 89.8%**. **Track A definition** → `pkg/definition` (Kind-discriminated `Envelope`, `Decode`/`Load`, total `validate` with `TypeResolver` type-refs INV-4, `CheckIncludeDAG` 3-colour DFS INV-5, `Command`-only `Instantiate` via `CommandSink` INV-2, infallible-after-validate INV-1, `ActionRegistry`); decoupled from TypeRegistry/World via consumer interfaces; **88.5%**. **T-6T06** → `examples/{config,window,diagnostic,ui}/` each `run()` hash-stable ×20 (definition decode+instantiate+INV-5, window lifecycle+event-replay+INV-2, diagnostics INV-1 gate+gizmos, flexbox+dirty-gate). **C29 P6 gate CLOSED.** Full workspace `go build`/`go test` (58 pkgs) + `modernize` clean; C-005 `-race` unavailable (CGO absent). Deferred to App integration: ECS-system wirings (window sync, diagnostic overlay/built-ins, definition hot-reload, ui PreUpdate) + real TTF rasterizer (x/image/font ADR). **Handoff: `/magic.task`** to promote the 12 cohort specs Draft → Stable.
<!-- Phase 4 granular task decisions (T-4A01..04, B/C/D01-03/E01-02, 2026-05-19→28) archived → PLAN.md Document History v1.12.0 (Phase 4 Done). -->
<!-- Phase 1–3 decisions archived (Done; see PLAN.md Document History + archives/tasks/). -->

## Blockers

<!-- Empty if none. Format: [severity] description -->

<!-- [resolved 2026-05-28] render-core RFC blocker — ratified RFC → Stable via /magic.spec; Phase 5 gate cleared. -->
<!-- [resolved 2026-05-28] P5 L1-only blocker — 5 L2 Go contracts authored via /magic.spec; full L1+L2 parity. -->
- [advisory] **8 P4 render specs may have empty Canonical References.** Promoted via task.md relaxed-MVC (not spec.md strict checklist). For full spec.md compliance, populate `## Canonical References` on mesh/materials/camera/post L1+L2 (impl exists in `pkg/render/...`). Non-blocking; hygiene pass.
<!-- [resolved 2026-05-31] asset-formats held Draft → narrowed (Option A) + promoted Draft → Stable via /magic.task. Stable surface = glTF loader (INV-2/INV-4) + stdlib PNG/JPEG + WAV; HDR/DDS/KTX2/OGG/MP3/FLAC/fonts/`.scene.json` + glTF animations/skins/morph/KHR explicitly marked Planned (no overclaim). Canonical Refs populated. INDEX v2.39.0 (Stable 74). Optional follow-on: pkg/asset/formats/scene/ to graduate the scene.json Planned item. -->

## Blocking Constraints

<!-- Anti-patterns discovered through real failures. MANDATORY reading. -->
<!-- Agent MUST explicitly acknowledge each constraint before working. -->

- [C-001] **C29 Promotion Gate**: No P1 spec may be promoted Draft → Stable until `examples/ecs/poc/` validates the runtime end-to-end (T-1T05).
- [C-002] **STOP FACTOR (Phase ≥ 4 Hold)**: Phases 4–8 stay in `Hold` until Phase 1 POC is validated AND Phase 2 App Framework reaches `Stable`. No premature implementation work in those subsystems.
- [C-003] **C24 Stdlib Priority**: Engine core MUST have zero external Go deps. Any third-party package requires explicit justification recorded in an ADR.
- [C-004] **C27 GC Compensation**: Hot-path allocations (commands, events, transient views) MUST flow through `sync.Pool`. Validation Track verifies ≤1 alloc/op for `BenchmarkCommandFlush`.
- [C-005] **C28 Race Gate**: All concurrent tests MUST pass with `-race`; CI blocks otherwise.

## Session Continuity

**Last Session Ended:** 2026-05-31 (late)
**Handoff File:** none
**Bootstrap Mode:** true (P5 = 10/10 Stable — asset-formats narrowed → Stable; P6 engine-core cohort = 12 specs Stable; P6 tooling F-CLI + editor N/H/O/P cores + glTF loader implemented; P6 tooling E/I/J/L/M + editor-bridge wiring + P7/P8 remain `[Bootstrap]`)
