# Project State

<!-- STATE.md — live project memory. Read FIRST in every workflow session. -->
<!-- Maximum 100 lines. Agent updates AFTER each completed action. -->

**Workspace:** main
**Updated:** 2026-06-01
**Phase:** 6 — UI, Tooling & Quality
**Status:** Active (engine-core 12 Stable; **P6 tooling E/F/I/J/L/M done — Track J fully complete (J01+J02)**; editor/graph cores N+H+O+P + P04 editor-bridge contract done; asset-formats Stable, P5 = 10/10 Stable. Remaining P6: engine-side editor wiring [App integration] + scene.json/T-6M02 follow-ons)

## Current Position

- **Task:** `/magic.run` — **Track J02 api-diff: `cmd/apidiff`** (Done; Track J complete). Snapshots the exported API of `pkg/...` via `go/ast`+`go/printer` (stdlib, C-003; signatures printer-rendered so reformatting ≠ diff), compares to committed `api/snapshot.json`, fails on removed/changed exports (breaking) — added is non-breaking. `-update` rewrites; `-json` for CI. **85.5% cov**; dogfooded `api/snapshot.json` (35 packages, self-compare clean). Fourth "committed snapshot → drift" tool (with benchcompare/examplecheck/releasenotes). `go test ./...` 70 pkgs green, build/modernize clean.
- **Next Action:** **`/magic.run`** — standalone tooling now exhausted (E/F/I/J/L/M done). Remaining P6: (a) **`pkg/asset/formats/scene/`** (.scene.json → `DynamicScene` via the Phase 3 reflection codec) — the cleanest remaining standalone, graduates an asset-formats Planned item; (b) **engine-side editor wiring** (App-integration pass): concrete `GraphEditorPlugin`/`NodeRegistryQuery`/`GraphDebugger` impls + `graphDebugSyncSystem`, OOP loader (N04), SSE/dispatch (O03/H02); (c) **T-6M02** component/plugin boilerplate gen. Or **`/magic.task`** to assess P6 status + weigh promotions (multi-repo precedent: `l2-visual-graph-editor-bridge` contract+guard green ⇒ promotable despite deferred wiring).

## Progress

```
Phase 1: [27/27] ████████ 100% ✓ Done
Phase 2: [24/24] ████████ 100% ✓ Done
Phase 3: [18/18] ████████ 100% ✓ Done
Phase 4: [19/19] ████████ 100% ✓ Done  ← all 10 specs Stable (render-core ratified)
Phase 5: [18/18] ████████ 100% ✓ Done  ← 10/10 specs Stable (asset-formats narrowed → Stable 2026-05-31)
Phase 6: [17/17] ████████ 100% ✓ engine-core cohort — 12 specs Stable (C29 gate closed)
Overall: [123/123] ████████ 100% 🎉 (engine-core cohort; P6 tooling+editor tracks deferred)
```

<!-- Phase 6 engine-core cohort = 17 tasks (A:3 B:2 C:3 D:3 G:2 K:3 + T-6T06) ALL DONE. Full P6 also has deferred tooling (E/F/I/J/L/M) + editor (H/N/O/P) tracks — not in the activated cohort. -->

## Recent Decisions

<!-- Last 3-5 locked decisions. Older entries → archived to PLAN.md -->

- 2026-06-01 **`/magic.run` — Track J02 api-diff: `cmd/apidiff` (Done; Track J complete)** — Built the API-stability gate completing Track J. `cmd/apidiff` snapshots the exported surface of `pkg/...` via `go/ast`+`go/printer` (stdlib-only, C-003 — avoids the `golang.org/x/tools/cmd/apidiff` third-party dep): `extract.go` (`Symbol` = name/kind/signature; exported func/method/type/const/var; excludes unexported names + methods on unexported receivers; handles generic receivers + type aliases; signatures **printer-rendered + whitespace-normalized** so reformatting is never a spurious diff), `snapshot.go` (`Snapshot` JSON + `scanPackages` walk, skips `_`/`.`/testdata/_test.go), `diff.go` (`Diff` matches by kind+name → **removed/changed = breaking**, added = minor), `main.go` (`-pkgs`/`-snapshot`/`-update`/`-json`, exit 1 on breaking). **85.5% cov**; dogfooded — `api/snapshot.json` captures **35 packages**, self-compare clean. The fourth "committed snapshot → drift" member: `pkg/version` defines what *breaking means* (caret SemVer), apidiff *detects* it in the real source. Bugfix during impl: `go/printer` renders `*ast.FuncType` without the name (it lives on `FuncDecl`), so the signature is assembled as `"func "+name+trimPrefix(render,"func")` — required regenerating the snapshot after the fix. `go test ./...` **70 pkgs green**, build/modernize clean. Path reconciled `scripts/api-diff/` → `cmd/apidiff`. **Track J fully complete (J01 pkg/version + J02 apidiff); all P6 tooling tracks done.** **Handoff: `/magic.run`** scene.json loader / engine-side editor wiring, or **`/magic.task`** to weigh P6 promotions.
- 2026-06-01 **`/magic.run` — Track P04 visual-graph editor-bridge contract (contract surface Done; engine wiring deferred)** — Implemented the editor↔engine graph "door" as a contract surface, mirroring the ratified multi-repo boundary. `pkg/editor/graph.go`: `GraphEditorPlugin` (open/close/select/connection/property, mutating callbacks return advisory `ValidationResult` before commit), `NodeRegistryQuery` (palette/search/compatible-pins/type-hierarchy), `GraphDebugger` (breakpoints/stepping/trace/pin-values) + editor-local DTOs. **Key decoupling decision:** followed the `definition.go` opaque precedent — the interfaces are self-contained (`uint64` entity IDs, `graphID string` instead of a `GraphDefinition` parameter, editor-local `NodeDescriptor`/`Connection`/`Direction`), so the external editor repo links a stable contract without importing the engine's `pkg/visualgraph` types. This keeps `pkg/editor` import-free of engine logic → passes `TestEditorPkgIsContractOnly` (no bodies/`internal`/`init`). `pkg/protocol/graph.go`: 4 IPC messages (`GraphBreakpointHit`/`GraphExecutionTraceEvent`/`GraphRuntimeError`/`GraphLiveUpdate`) + wire `GraphExecutionFrame` (distinct from the editor inspection type so protocol stays stdlib-only) + `GraphChangeType` + 4 additive `Decode` cases. **protocol 94.6% cov**; editor is contract-only (`[no statements]`) but guard-tested + a fake-implementer satisfies all 3 interfaces (self-connection validation rejected without mutation). `go test ./...` **69 pkgs green**, build/modernize clean; multi-repo guard suite (`TestEditorPkgIsContractOnly`/`TestProtocolStdlibOnly`/`TestProtocolRoundTrip`) green. l2-visual-graph-editor-bridge 0.1.0 → 0.2.0 (Canonical Refs populated, reconciliation noted; Status stays Draft pending wiring). **Deferred (App integration):** concrete impls + `graphDebugSyncSystem` (PostUpdate, editor-attached only). **Handoff: `/magic.run`** engine-side editor wiring / OOP loader (N04) / SSE+dispatch (O03/H02), or **`/magic.task`** to weigh promoting the bridge (contract+guard green, multi-repo precedent).
- 2026-05-31 **`/magic.run` — Track I examples framework: `cmd/examplecheck` (T-6I01/I02 Done) — completes all P6 tooling** — Built a committed-golden gate for the examples: `cmd/examplecheck` discovers `examples/*/main.go`, runs each, parses the `PASS:/FAIL/hash=` convention, and compares **hash-emitting** examples against `examples/goldens.json` (drift → exit 1) while **smoke** examples (multiple `PASS` lines, no single hash) are run-only. Key design: the exec boundary is an injected `exampleRunner func(name)(string,error)`, so `evaluate`/`classify`/compare are unit-tested with canned outputs (no subprocess) — `runExample` (`go run`) + `gitChangedPaths` stay thin integration. `-update` rewrites goldens; `-list` + `-changed <ref>` (git-diff → pure `examplesFromPaths`) drive selective CI builds (T-6I02). Added `examples/_template/` scaffold (the `_` prefix → skipped by both `go ./...` and `examplecheck`) + rewrote `examples/README.md` (conventions + golden gate). **87.2% cov**; **dogfooded** — ran all 16 examples via `-update` → `goldens.json` with 9 hashes (2d/animation/audio/config/diagnostic/gltf/tweening/ui/window; gltf hash matched the earlier glTF run — cross-checked), 7 smoke, 0 fail. `go test ./...` **69 pkgs green**, build/modernize clean. Rationale: the committed golden catches *deterministic behaviour drift* the per-example ×20 stability tests can't (they prove stability, not the value). Path reconciled `scripts/examples/` → `cmd/examplecheck` (testable Go tool). **All P6 tooling tracks E/F/I/J/L/M now done.** **Handoff: `/magic.run`** editor-bridge wiring (App integration) or `/magic.task` to assess P6 status.
- 2026-05-31 **`/magic.run` — Track M codegen: query arity-ladder generator (T-6M01 Done, T-6M02 deferred)** — Built `cmd/codegen/query`, which generates the typed `Query`/`Tuple` arity ladder. Rationale: Go generics have **no variadic type parameters**, so each arity (`Query2`, `Query3`, …) must be a distinct type — the hand-written `Query1–Query3` set the pattern and the ladder previously **capped at 3**. The generator's `genArity(n)` emits a `TupleN` + `QueryN` + `NewQueryN`/`State`/`All`/`Count`/`refresh` block structurally identical to `query2.go`, and `genSource` runs the whole emission through `go/format.Source` (which doubles as a syntax validator — malformed output fails here, not at compile time). CLI `-min`/`-max`/`-out` + range validation. **95.2% cov** (generator) + a re-parse assertion on output. Ran it (`//go:generate` directive in `query.go`) to produce `internal/ecs/query/query_gen.go` — `Query4/Query5/Query6` + `Tuple4-6`, "DO NOT EDIT" header — accessing the package's unexported helpers (`componentIDFor`/`applyFilters`/`fetchComponent`/`errSameTypeInQuery`); added `pkg/ecs` re-exports (`Query4-6`/`NewQuery4-6`). **Extends the query ladder 3 → 6.** Functional `query_gen_test.go` proves Query4/5 spawn→`Count`→`All` tuple values→pointer-mutation-persists→same-type rejection. `go test ./...` **68 pkgs green**, build/modernize clean (generated `Count` loop matches `query2/3`, not flagged); C-005 `-race` unavailable (CGO absent). Generated code is additive — `Query1-3` untouched, no regression. T-6M02 (component/plugin boilerplate) deferred — the Track F CLI `scaffold` already emits basic stubs. **Handoff: `/magic.run`** — Track I (examples framework) / editor-bridge wiring.
- 2026-05-31 **`/magic.run` — Track E build tooling: CI workflow + `cmd/releasenotes` (T-6E01/E02/E03 Done)** — **CI (`.github/workflows/ci.yml`):** `test` (build+vet+`-coverprofile` over ubuntu/macos/windows matrix, Go pinned via `GO_VERSION` env tracking `pkg/version.MinGoToolchain`), `race` (`go test -race ./...` on ubuntu — **first place C28/C-005 actually runs**, since hosted runners have CGO/gcc absent locally), `lint` (golangci-lint-action), `bench` (T-6E02 — pipes `go test -bench -benchmem ./...` through `cmd/benchcompare`; `continue-on-error` advisory because the seed `baseline.json` ns/op is dev-machine-specific while allocs/B are the portable signal). **`cmd/releasenotes` (T-6E03, Go — path reconciled from `scripts/release/` for testability):** `parse.go` (spec title/version/status + Document History table parser; em-dash/separator rows skipped, pipes-in-desc preserved, missing-H1 → filename fallback), `report.go` (`ReleaseNotes` dated changelog + `BreakingChanges` **dogfooding `pkg/version`** — a transition is breaking iff `^prev` doesn't match latest, the same caret rule as plugin compat, so 0.x minor counts), `main.go` (`-specs`/`-since`/`-breaking`/`-o`). **92.1% cov**; dogfooded on the real `.design/main/specifications` corpus. `go test ./...` **67 pkgs green**, build/modernize clean. Anti-hallucination: bench-gate is a CI job step over the tested Go tool, not an ad-hoc shell runner. **Track E core complete** — P6 tooling now F+J+L+E done; remaining: M (codegen), I (examples framework), editor-bridge wiring. **Handoff: `/magic.run`** — Track M / Track I / editor-bridge.
- 2026-05-31 **`/magic.run` — Track L benchmark drift gate: `cmd/benchcompare` (T-6L01 Done, T-6L02 partial)** — Built a stdlib-only benchmark regression gate: `parse.go` (parses `go test -bench -benchmem` output via a small documented-format scanner — no `x/perf` dep, C-003 — stripping the GOMAXPROCS `-N` suffix to a canonical name), `baseline.go` (`Baseline` JSON load/save keyed by name), `compare.go` (`Compare`: **ns/op + B/op** regress past `-threshold` %; **allocs/op regress on ANY increase** — a 0→1 hot-path jump is categorical, encoding C-004/C-027), `main.go` (flags `-baseline`/`-current`/`-threshold`/`-update`/`-json`; exit 0/1/2 = OK/regression/usage; JSON sanitizes non-finite Δ to null since `encoding/json` rejects Inf/NaN). **85.4% cov** (synthetic-output golden = the tool's C29 analogue). **Dogfooded end-to-end:** piped real fast benchmarks through `-update` → committed `bench/baseline.json` (16 hot-path entries, all 0 B/0 allocs) + `bench/README.md`; self-compare → OK exit 0. `go test ./...` **66 pkgs green**, build/modernize clean; C-005 `-race` unavailable (CGO absent). Chose Track L over Track M (codegen) — a tool bound to the *stable benchmark-output text contract* has zero engine-API coupling vs a generator that must track the ECS Query API. Per-subsystem `bench/{subsystem}/` packages: benchmarks stay colocated in `_test.go` (harness consumes piped output). T-6L02 CI shell (`scripts/bench-gate/`) deferred to Track E (T-6E02). **Handoff: `/magic.run`** — Track M codegen / Track E CI (natural next: wires this gate) / editor-bridge.
- 2026-05-31 **`/magic.run` — Track J compat: `pkg/version` SemVer kernel extracted (T-6J01 Done, T-6J02 deferred)** — Created stdlib-only **`pkg/version`** as the single source of truth for SemVer: `version.go` (`Version` + `Parse` [v-prefix/pre-release-strip, partial→0] + `Compare` + `String`), `constraint.go` (`Constraint` + `ParseConstraint` Cargo-subset grammar — caret/tilde/`>=`/`>`/`<=`/`<`/`=`/bare/comma-range, caret-on-0.x pins minor — + `Matches` + `IsAny`), `policy.go` (documented engine SemVer policy + `MinGoToolchain` = "1.26.3" from go.mod + `GoToolchainConstraint`/`IsGoToolchainSupported`, dogfooding the constraint evaluator). **DRY extraction:** `pkg/plugin/{semver,constraint}.go` (Track N) refactored from ~190 lines of duplicated logic to **type-alias re-exports** (`type Version = version.Version`, `type Constraint = version.Constraint`, wrapper `ParseVersion`/`ParseConstraint`, `var ErrInvalidVersion = version.ErrInvalidVersion`) — alias `=` preserves type identity so the SDK public API + all Track N tests pass unchanged (verified via `errors.Is` on the shared sentinels). **pkg/version 98.9% cov**; pkg/plugin 86.1% (logic moved out, still >80%), internal/plugin 93.0%, cmd/cli 85.0% all green; `go test ./...` **65 pkgs**, `go build`/modernize clean; C-005 `-race` unavailable (CGO absent). T-6J02 (`scripts/api-diff/` public-surface snapshot + breaking-change CI gate) deferred — it's a CI/tooling deliverable. **Handoff: `/magic.run`** — Track M codegen / editor-bridge / tooling E/I/L.
- 2026-05-31 **`/magic.task` — asset-formats narrowed + promoted Draft → Stable (Option A)** — User chose to scope the spec to the implemented surface rather than build scene.json first. Narrowed `l1-asset-formats` (0.1.0→0.2.0) + `l2-asset-formats-go` (0.1.0→0.2.0): the **Stable surface** is the glTF 2.0 loader (value-based fan-out of scene/mesh/material/texture + deterministic `GltfAssetLabel`, INV-2/INV-4; 90.0% cov + fuzz + `examples/gltf` ×20) + stdlib PNG/JPEG + PCM WAV. HDR/DDS/KTX2/BMP/WebP/TGA/OGG/FLAC/MP3/AAC/fonts/`.scene.json` + glTF animations/skins/morph/KHR are explicitly marked **Planned** (deferred future loaders) so the spec no longer overclaims. **Canonical References populated** on both (gltf label/schema/convert/loader + test, image, audio, example). L2 corrected to the shipped value-fan-out (handle-based `GltfAsset` needs a `LoadContext` the real `AssetLoader.Load(r,settings)` lacks → App-integration); INV-1 cross-package conflict-detection + INV-3 build-tag optional formats noted as planned hardening. **Pre-Planning Stabilization: 2 promoted, 0 other newly eligible.** Also reconciled the stale Phase 5 PLAN checklist (all 10 P5 specs were Stable in INDEX but shown `[ ] Draft [Bootstrap]`) → `[x] Stable`; Phase 5 header `Active` → `Done`. **C24 Skeptic clean:** asset-formats is a downstream consumer (mesh/material/scene/audio) — no Stable spec `Implements` it, so no broken Stable→Draft edge; layer-clean (L1 promoted before L2). INDEX v2.38.0 → v2.39.0 (Stable 72 → 74, Draft 40 → 38); PLAN/TASKS v1.23.0. **Handoff: `/magic.run`** — P6 tooling (Track M/J) / editor-bridge / tooling E/I/L; optional scene.json follow-on.
- 2026-05-31 **`/magic.run` — glTF loader core (T-5B03 partial; clears most of the asset-formats hold)** — Implemented `pkg/asset/formats/gltf/` (stdlib-only, dep-free C-003): `GltfKind`/`GltfAssetLabel` deterministic `(Kind,Index)` addressing (INV-4); glTF 2.0 JSON subset schema + OpenGL component/mode constants; buffer resolution (base64 data-URI + GLB BIN chunk) + accessor de-interleave with exhaustive bounds-checking; **one `mesh.Mesh` per glTF primitive in declaration order** (POSITION/NORMAL/UV0/UV1/TANGENT/COLOR + u8→u16 widening / u16 / u32 indices + topology map); PBR `material.Material` (base-color/metallic/roughness/emissive factors + alpha-mode + double-sided); embedded data-URI/bufferView textures → `renderimage.Image` (reuses `renderimage.DecodeImage`, no duplication); `GltfLoader` `AssetLoader[GltfAsset,LoadSettings]` (.gltf/.glb auto-detect) + `Decode` with panic-recover so malformed input is always a wrapped error, never partial (INV-2) + `GltfAsset.Get`/`Labels`/`MeshMaterial` + `RegisterAll`. **90.0% cov** + `FuzzDecode` (114k execs, 0 panics); `examples/gltf/` fan-out hash-stable ×20 (C29 golden). `go build ./...`/`go vet`/`modernize` clean; `go test ./...` **64 pkgs green**; C-005 `-race` unavailable (CGO absent). Architectural note: the L2 spec's handle-based `GltfAsset` (sub-asset registration via `LoadContext`) can't be honored by the real `AssetLoader.Load(r,settings)` signature — implemented a value-holding fan-out (slices addressed by stable label) which proves INV-2/INV-4 standalone; handle registration stays an App concern. Deferred: `scene.json` loader (`pkg/asset/formats/scene/`), animations/skins/morph-targets (accessor matrix/quat decode + `animation.AnimationClip` samplers), KHR extensions, external-URI resolution. **Handoff: `/magic.task`** to weigh `asset-formats` Draft → Stable (residual gate: scene.json round-trip from the spec's testing strategy).
- 2026-05-31 **`/magic.run` — Track F CLI Tooling (T-6F01/02 Done, F03 partial)** — Replaced the `cmd/cli` Phase-0 stub with a working CLI: stdlib-`flag` `Router` + `Command` interface + `Output` (text vs `--json` stable output INV-4) + global flags (`--json`/`--force`/`--dry-run`) + no-arg structured help (INV-2) + gated command registry (only implemented commands listed, INV-3). Commands: `doctor` (env/version, --json), overwrite-safe `scaffold <component|system|plugin>` (skips existing unless `--force`, `--dry-run` previews, INV-1), `plugin validate <path>` + `plugin list <dir>` consuming the `pkg/plugin` SDK (`ParseManifest`+`Validate`+`Mode`). This makes the Track N plugin SDK usable end-to-end. **85.0% cov**; `go build ./...` clean, `go test ./...` 62 pkgs green, modernize clean. Deferred: `new project` template tree, `asset import|list|build`, full `plugin install/enable/disable/info/remove/doctor` (need templates + asset pipeline + plugin install flow). **Handoff: `/magic.run`** glTF / codegen.
- 2026-05-31 **`/magic.run` — Track P Visual Graph System core (T-6P01/02/03 Done, P04 deferred)** — Implemented `pkg/visualgraph` (runtime, not editor-gated): graph data model (`GraphDefinition`/`Node`/`Pin` with Direction+Kind/`Connection`/`VariableDecl` + lookup helpers), `NodeRegistry` + `NodeDescriptor`/`PinDescriptor` (Register/Get/List/ListByCategory/Search, deterministic sort), `ValidateGraph` (endpoint/direction/kind checks + type-compat INV-3 + 3-colour DFS data-dependency cycle detection INV-4), `Interpreter` (execution-pin chain following + lazy memoized data-input pull, node-step-limit INV-2, `CommandSink` for all mutations INV-1, deterministic given same graph+sink INV-4) + a representative built-in node set (event.OnUpdate, math.Add, logic.Greater, flow.Branch, action.Log/SetVariable covering every execution category). **89.3% cov**; `go build ./...` clean, `go test ./...` 61 pkgs green, modernize clean; C-005 `-race` unavailable (CGO absent). Deferred: editor gateway (`pkg/editor/graph.go` + `pkg/protocol/graph.go`, the l2-visual-graph-editor-bridge contract), TypeRegistry auto-generation of component/event/state nodes, SubGraph/Query node categories, the `"graph"` definition-loader wiring — all need App/editor integration. **Handoff: `/magic.run`** tooling tracks / glTF.
- 2026-05-31 **`/magic.run` — Track O AI API Plugin core (T-6O02/04/05 Done, O01/03 partial)** — Implemented `pkg/plugins/aiapi` (`//go:build editor`): canonical request/response/message/usage types, `Provider` interface + compile-time registry + `Select` (extension point), OpenAI-compatible `provider_openai` with pure `buildOpenAIRequest`/`parseOpenAIResponse` + full `Complete` validated via `httptest` (success + 500→`CodeHTTP5xx`), `Config`/`DefaultConfig`, per-provider `rateLimiter` token bucket (RPM+TPM, injectable clock, INV-8), `E-PLUGIN-AIAPI-{NNN}` codes + `MapHTTPStatus` (401/429/4xx/5xx, INV-5), env credential source + `errs.Redactor` reuse + `crypto`-style secret zeroing (INV-1, keyring/age ADR-gated), `FakeProvider` deterministic responder (INV-7 foundation). **82.3% cov (`-tags editor`)**; default `go build`/`go test ./...` 60 pkgs green (editor excluded); `go build -tags editor ./...` compiles the whole editor surface; modernize clean (string-concat → strings.Builder via -fix). Reuses `pkg/errs.Redactor` cross-package (no duplication). Deferred (App/event/diag/OOP wiring): SSE streaming, 8-method dispatch, plugin lifecycle, diagnostics/cost events, full in/out-of-process parity harness, Anthropic/Gemini/local providers. **Handoff: `/magic.run`** Track P / tooling / glTF.
- 2026-05-31 **`/magic.run` — Track H AI Assistant System (T-6H01/03 Done, H02 partial)** — Implemented `pkg/assistant` (`//go:build editor` — first build-tag-gated editor package, establishing the pattern): `AgentMessage` MCP-shaped JSON protocol + Encode/Decode, `Capability` bitfield (Read/Write/Advanced) + String, standard-method names + required-capability table, `Transport`/`Connection` interfaces + `StdioConnection` (newline-delimited JSON) + `MemConnection` (deterministic test responder), `AssistantManager.Dispatch` (INV-2 capability gate with logged denials, INV-5 `context.WithTimeout`/cancel so a dead agent never blocks the editor, request log) + `ApplyModifications` agent+request tagging for one-undo-group edits (INV-1/4). **88.0% cov under `-tags editor`**; default `go build ./...`/`go test ./...` 60 pkgs green (assistant excluded by build tag — verified the editor gate doesn't affect shipped builds); modernize clean (via `GOFLAGS=-tags=editor`); C-005 `-race` unavailable (CGO absent). Deferred: websocket/http transports (ws dep ADR), `ContextProvider` EditorContext assembly, editor UI panels (editor-repo). Unblocks Track O (ai-api-plugin reuses these transports for OOP + implements the assistant methods). **Handoff: `/magic.run` Track O.**
- 2026-05-31 **`/magic.run` — Track N plugin-distribution SDK core (T-6N01/02 Done, N03 partial)** — Implemented the editor-layer SDK root: `pkg/plugin` (`Plugin`/`PluginGroup` re-export from app, `PluginID`/`Mode`/`State` enums, `Version` + `Constraint` Cargo-subset SemVer with caret-on-0.x semantics — INV-2, `Manifest` + stdlib-only TOML-subset `ParseManifest` + `Validate` — INV-1, `Capability` vocabulary + `CapabilitySet` with tiers + wildcard match — INV-3, `PluginContext`/`CommandIssuer`, `E-PLUGIN` sentinels + `CapabilityError`; **no Go `plugin` .so** per L1 §2) + `internal/plugin/Manager` (registry, INV-2 compatibility resolution, INV-5 duplicate-ID guard, lifecycle states) + `capabilityProxy` (INV-3 runtime enforcement, logged denials) + plugin-ID-tagged commands (INV-7). **pkg/plugin 88.5%, internal/plugin 93.0% cov**; `go build ./...` clean, `go test ./...` 60 pkgs green, modernize clean; C-005 `-race` unavailable (CGO absent). Deferred to App integration: discovery (4 sources), capability-prompt persistence, in-process Build/Ready/Finish wiring, and T-6N04 OOP loader (subprocess spawn + `pkg/protocol` lifecycle, needs Track H transports). Unblocks Track O (ai-api-plugin) + Track F (cli plugin subcommands) + Track M (plugin scaffold). **Handoff: `/magic.run`** for Track O/H.
- 2026-05-31 **`/magic.task` — P6 editor-layer L2 tier mapped (registry sync)** — Placed the 5 new editor-layer L2 specs into PLAN.md Phase 6 under their L1 parents: `l2-{plugin-distribution,ai-assistant-system,ai-api-plugin,cli-tooling,definition-integration}-go` (all `[L2] [Bootstrap]`). Resolves Pre-flight ORPHANED_SPEC × 5 + SYNC_GAP (PLAN was on INDEX v2.37.0, registry at v2.38.0). Annotated `phase-6.md` Track Overview A/F/H/N/O with their L2 refs. Pre-Planning Stabilization: **0 promoted** — the 5 L2s are layer-blocked (Draft L1 parents); editor-layer L1s are Draft concept specs with no impl/examples (C29). 5 hard L2→L1 edges (37 total), acyclic. **Full P6 L1+L2 parity achieved — spec surface complete** (governance L1s compatibility-policy/examples-framework need no L2). PLAN/TASKS v1.22.0, INDEX unchanged (v2.38.0). **Next:** implementation phase — `/magic.run` tooling or editor tracks, or glTF for asset-formats.
- 2026-05-30 **`/magic.spec` — Phase 6 editor-layer L2 tier authored (5 contracts)** — Created `l2-plugin-distribution-go` (public `pkg/plugin` SDK no-`internal/`, `plugin.toml` manifest + SemVer Constraint, `CapabilitySet` + runtime `capabilityProxy`, in-process + OOP loaders sharing one lifecycle, OOP failure isolation over `pkg/protocol`, plugin-ID-tagged commands — INV-1..9; no Go `plugin` .so), `l2-ai-assistant-system-go` (`//go:build editor`: `AssistantManager` + JSON `AgentMessage` over stdio/ws/http, `Capability` bitfield, Command-pipeline tagged undo, worker-pool async cancel/timeout), `l2-ai-api-plugin-go` (`pkg/plugins/aiapi` Provider+canonical types, env/keyring/age credentials with zeroing+redaction, rate limiter, `E-PLUGIN-AIAPI` codes, FakeProvider mode-parity), `l2-cli-tooling-go` (stdlib-flag Router + gated Command registry, overwrite-safe scaffold, `--json`, `ecs plugin` subcommands), `l2-definition-integration-go` (stable `pkg/editor` `DefinitionEditorInterface` + order-based serialization + entity hooks). All `Draft` (L1 parents Draft → layer constraint). Unblocked by multi-repo Stable. 5 new hard L2→L1 edges (37 total), acyclic; dependency order `{plugin-dist ‖ ai-assistant} → ai-api-plugin`, `cli → plugin-dist`, `definition-integration → definition`. **Completes P6 editor-layer L1+L2 parity.** INDEX v2.37.0 → v2.38.0 (112 specs, Stable 72/Draft 40). Post-Update Review (Critic): Invariant Compliance maps each L1 INV to a concrete Go mechanism, contracts only (no impl code), `Implements`/`Related` links valid, C-003 honored (TOML/age/keyring/WS deps explicitly ADR-gated, not silently assumed). **Handoff: `/magic.task`** to map into Phase 6.
- 2026-05-30 **`/magic.task` — P6 engine-core cohort stabilized (12 specs Draft → Stable)** — Pre-Planning Stabilization promoted **6 L1 + 6 L2** (error-core, platform-system, window-system, diagnostic-system, ui-system, definition-system) after re-verifying the C29 P6 gate (`examples/{config,window,diagnostic,ui}` hash-stable ×20 + `go test ./...` 58 pkgs green). All 6 L2 specs' **Canonical References populated** with on-disk-verified source + test + example files. C24 Skeptic cleared: every cohort `Implements` parent is now Stable, and the intra-cohort edges (diagnostic→error-core, definition→ui) are both Stable → no broken Stable→Draft edge; `asset-formats` stays held Draft (nothing in the cohort depends on it). Deferred App-integration glue (window sync, diagnostic overlay/built-ins, definition hot-reload, ui PreUpdate) + the x/image/font ADR are non-blocking (analogous to render-core's nopBackend). INDEX v2.36.0 → v2.37.0 (Stable 60 → 72, Draft 47 → 35), PLAN/TASKS v1.21.0. **Phase 6 cohort = 12 specs Stable.** **Handoff: `/magic.spec`** for the editor-layer L2 tier.
- 2026-05-30 **`/magic.run` — Phase 6 engine-core cohort COMPLETE (17/17, C29 gate closed)** — Run 3 (top layer + validation). **Track D ui** → `pkg/ui` (Style+Val union, Node/LayoutRect/ZIndex, widgets, Interaction/MouseFilter/Focused) + `internal/ui` (single-line flexbox `Solve` with grow/shrink/justify/align/padding/gap/reverse/nested, `SolveIfDirty` dirty-gate INV-1, `FontAtlas` over render-core `DynamicAtlas` INV-4, `HitTest`+`InteractionFor` INV-3, `UiFeature`+`SortByZ` INV-2); **pkg/ui 95.5%, internal/ui 89.8%**. **Track A definition** → `pkg/definition` (Kind-discriminated `Envelope`, `Decode`/`Load`, total `validate` with `TypeResolver` type-refs INV-4, `CheckIncludeDAG` 3-colour DFS INV-5, `Command`-only `Instantiate` via `CommandSink` INV-2, infallible-after-validate INV-1, `ActionRegistry`); decoupled from TypeRegistry/World via consumer interfaces; **88.5%**. **T-6T06** → `examples/{config,window,diagnostic,ui}/` each `run()` hash-stable ×20 (definition decode+instantiate+INV-5, window lifecycle+event-replay+INV-2, diagnostics INV-1 gate+gizmos, flexbox+dirty-gate). **C29 P6 gate CLOSED.** Full workspace `go build`/`go test` (58 pkgs) + `modernize` clean; C-005 `-race` unavailable (CGO absent). Deferred to App integration: ECS-system wirings (window sync, diagnostic overlay/built-ins, definition hot-reload, ui PreUpdate) + real TTF rasterizer (x/image/font ADR). **Handoff: `/magic.task`** to promote the 12 cohort specs Draft → Stable.
- 2026-05-30 **`/magic.run` — Phase 6 engine-core mid-layer (C+B Done, 10/17)** — Run 2 of the cohort. **Track C diagnostic** → `pkg/diag` (`RingBuffer[T]` fixed-cap 0-alloc Push + deterministic averages, `DiagnosticsStore` with `HasAnyReader`/per-metric reader gate INV-1, `GizmoBuffer` 8-shape immediate-mode + 0-alloc `Reset` reuse, `ModuleFilterHandler` over `slog`, `profiling`/noop `Span` split INV-4) + `internal/diag/GizmoFeature` (RenderFeature, headless Draw + Flush reset INV-2); **pkg/diag 94.6%, internal/diag 100%**. **Track B window** → `pkg/window` (Window component + WindowMode/PresentMode/Cursor enums + `WindowResolution.Logical`, `PrimaryWindow` marker, `WindowBackend` iface, `RawWindowHandle`/`WindowDescriptor`/`WindowDiff`, `PlatformEvent` + `CausesAppExit` INV-2, `ExitCondition`, `WindowPlugin`) + `internal/window` (`HeadlessWindowBackend` deterministic call-log + scripted event replay, `DiffWindow` pure field-diff INV-4 with Focused excluded, `PrimaryWindowRes` + `CheckSinglePrimary` INV-1); **pkg/window 89.3%, internal/window 82.6%**. `go build ./...` + `go test ./...` green; `modernize` clean; C-005 `-race` unavailable (CGO absent). Deferred to App-integration wiring: diagnostic built-in metrics + debug overlay (needs ui), `windowSyncSystem`/`PollEvents` (needs Commands+Query+change-detection). **Remaining cohort:** D ui (heaviest), A definition, T-6T06 (C29 gate). **Handoff: `/magic.run main`.**
- 2026-05-30 **`/magic.run` — Phase 6 engine-core foundation layer (K+G Done, 5/17)** — Implemented the two cohort roots (no intra-cohort deps): **Track K error-core** → `pkg/errs` (`EngineError` over stdlib `errors`, range-partitioned `Code` registry with duplicate/out-of-range guards, `Severity`/`Audience` total-switch enums, `fs.FS` `Catalog` with bare-code fallback, `//go:build debug` trace split, `MustSucceed` Fatal+Developer-only panic, `Redactor`; `locales/errors.en.json`; **89.3% cov**); **Track G platform** → `pkg/platform` (immutable `PlatformProfile`, branchless `PlatformCaps`, OS/Arch/Tier total-switch enums, `PlatformPlugin` iface) + `internal/platform` (pure `osFromGOOS`/`archFromGOARCH`, `!headless`/`headless` profile split with INV-4 caps exclusion, profile-inserting `Plugin`; **100% cov in both builds**, headless build verified). `go build ./...` + `go test ./...` green; `modernize` clean; C-005 `-race` unavailable in env (CGO absent — as P4/P5). Checkpointed at the foundation layer to preserve the 80%-coverage/quality discipline rather than rush the heavier mid/top tracks (mirrors P5's multi-run execution). **Remaining:** C diagnostic, B window, D ui (heaviest), A definition, T-6T06 (C29 gate). **Handoff: `/magic.run main`.**
- 2026-05-30 **`/magic.task` — Phase 6 ACTIVATED (engine-core cohort)** — Per user direction, `Hold → Active` for tracks **A/B/C/D/G/K** (definition, window, diagnostic, ui, platform, error-core). Each now has an authored L2 contract treated as the authoritative design source. **Key work — reconciled the pre-existing phase-6.md decomposition to the L2 contracts** (the workbook predated them and had drifted): Track C dropped the wrong "counters/gauges/histograms" Prometheus model → spec's `DiagnosticsStore` + `RingBuffer` + gizmos + profiling; Track A reframed `template` as entity-prefab (not text-substitution — expressions are a deferred OQ); Tracks A/C/G file paths realigned to the L2 package layouts; per-track `Verify` lines added; **T-6T06** engine-core C29 validation track added (`examples/{config,window,diagnostic,ui}/`). Build order `{K‖G} → {B,C} → D → A`. Tooling (E/F/I/J/L/M) + editor-layer (H/N/O/P) deferred within the Active phase. Bootstrap retained (Draft L1+L2 promote after T-6T06). C24 Skeptic flags logged: UI under-sized (3 tasks for flexbox+widgets+interaction+atlas), diagnostic-overlay→UI soft-dep, x/image/font ADR gates UI C29. PLAN/TASKS v1.20.0, INDEX unchanged (v2.36.0). **Handoff: `/magic.run main`.**
- 2026-05-30 **`/magic.spec` — Multi-Repo Architecture ratified (Draft → Stable)** — `l1-multi-repo-architecture` Draft → **Stable** (v1.4.0 → v1.5.0) + `l2-multi-repo-architecture-go` Draft → **Stable** (v0.1.0 → v0.2.0). Mirrors the render-core ratification: the spec was deliberately demoted to Draft (v1.4.0) awaiting validating code + deferring editor-kickoff open questions; the engine-side boundary `pkg/editor/` + `pkg/protocol/` is now implemented and the **architecture-guard suite is green** (`go test ./pkg/editor/ ./pkg/protocol/`): `TestEditorPkgIsContractOnly` (INV-3), `TestNoEditorImports` (INV-1), `TestNoPackageInit`/`TestProtocol_NoPackageInit` (INV-5), `TestProtocolStdlibOnly` (INV-4), `TestProtocolRoundTrip` (all 8 wire Kinds) + unknown-kind/extra-field forward-compat — this guard suite IS the C29 validator the L2 spec names. Both Canonical References populated with on-disk sources + tests. L1 open questions 2–4 confirmed non-blocking (WebSocket additive, editor-versioning out of engine scope, no-op interfaces deferred). C12 check: only `Implements` child is `l2-multi-repo-architecture-go` (promoted) — no L3, layer-integrity intact. INDEX v2.36.0 (Stable 58 → 60, Draft 49 → 47). **Unblocks the P6 editor-layer L2 tier.** Post-Update Review (Critic): Invariant Compliance maps INV-1…6 to concrete guard tests, links valid, no impl code in specs. SYNC_GAP (PLAN on v2.35.0) → handoff `/magic.task`.
- 2026-05-30 **`/magic.task` — P6 UI/declarative tier mapped (registry sync)** — Placed the 3 new P6 L2 specs into PLAN.md Phase 6 under their L1 parents: `l2-definition-system-go`, `l2-diagnostic-system-go`, `l2-ui-system-go` (all `[L2] [Bootstrap]`). Resolves Pre-flight ORPHANED_SPEC × 3 + SYNC_GAP (PLAN was on INDEX v2.34.0, registry at v2.35.0). Annotated `phase-6.md` Track Overview A/C/D with their L2 refs. Pre-Planning Stabilization: **0 promoted** — the 3 L2s are layer-blocked (Draft L1 parents), all other Draft specs are Phase-6+ `Hold`-gated (C-002). 3 hard L2→L1 edges (35 total), acyclic. **P6 engine-core L2 coverage complete (6/6).** PLAN/TASKS v1.18.0 (history row added), INDEX unchanged (v2.35.0). **Note:** Phase 6 `hold_reason` (Phase 1–3 Stable) is now satisfied — activation is unblocked (decision point in Next Action).
- 2026-05-30 **`/magic.spec` — Phase 6 UI/declarative tier authored** — Created 3 L2 Go contracts building on the foundational trio: `l2-diagnostic-system-go` (DiagnosticsStore + fixed-cap `RingBuffer` 0-alloc/deterministic, `hasAnyReader` zero-cost gate INV-1, pooled gizmo `RenderFeature` INV-2, `Last`-only INV-3, build-tag `profiling` spans INV-4, `log/slog` per-module filter, error codes deferred to `l2-error-core-go`), `l2-ui-system-go` (`Style`+`Val` union, flexbox solver with dirty-subtree relayout via `Changed[Style]` INV-1, `PreUpdate` `Interaction` hit-test + MouseFilter + focus neighbors INV-3, `FontAtlas` glyph cache over render-core `DynamicAtlas` INV-4, `UiFeature` composites last + ZIndex INV-2, render-only `OffsetTransform`; flagged x/image/font ADR), `l2-definition-system-go` (Kind-discriminated `Envelope`, `DefinitionLoader` total validation → infallible instantiate INV-1, Command-only interpreters INV-2, `DefinitionInstance` hot-reload INV-3, registry-resolved values INV-4, include-DAG cycle reject INV-5, reuses `DynamicScene` codec + `NextState`). All `Draft` (L1 parents Draft → layer constraint). 3 new hard L2→L1 edges (35 total), acyclic. **P6 engine-core L2 coverage complete (6/6).** INDEX v2.35.0 (107 specs, Stable 58/Draft 49). Post-Update Review (Critic): Invariant Compliance substantive per INV, contracts only (no impl code), `Implements`/`Related` links valid, C-003 honored (x/image/font ADR explicitly deferred, not silently assumed). **Handoff: `/magic.task`** to map specs into Phase 6.
- 2026-05-30 **`/magic.task` — P6 trio mapped (registry sync)** — Placed the 3 new P6 L2 specs into PLAN.md Phase 6 under their L1 parents: `l2-window-system-go`, `l2-platform-system-go`, `l2-error-core-go` (all `[L2] [Bootstrap]`). Resolves Pre-flight ORPHANED_SPEC × 3 + SYNC_GAP (PLAN was on INDEX v2.33.0, registry at v2.34.0). **Drift fix:** the freshly-authored `l2-error-core-go` used `pkg/errcore/` while `phase-6.md` had already committed `pkg/errs/` across 4 references (Track K decomposed into T-6K01..03 with concrete file paths) — aligned the spec to the more-committed workbook path. Annotated `phase-6.md` Track Overview B/G/K with their L2 refs. Pre-Planning Stabilization: **0 promoted** — the 3 L2s are layer-blocked (Draft L1 parents), all other Draft specs are Phase-6+ `Hold`-gated (C-002) with no C29 validation track. 3 hard L2→L1 edges (32 total), acyclic; `window↔platform` soft cycle non-blocking. Phase 6 stays `Hold`. PLAN/TASKS v1.17.0 (backfilled the missing 1.16.0 history row), INDEX unchanged (v2.34.0). **Handoff: `/magic.spec`** for the next P6 L2 tier (ui/definition/diagnostic).
- 2026-05-28 **`/magic.spec` — Phase 6 foundational trio authored** — Created 3 L2 Go contracts: `l2-error-core-go` (EngineError over stdlib `errors`, Severity/Audience total-switch enums, range-partitioned Code registry, `fs.FS` locale catalog + default fallback, build-tag debug traces, Fatal+Developer-only panic), `l2-platform-system-go` (immutable `PlatformProfile` resource, `PlatformCaps` branchless bitfield, `//go:build`-isolated per-OS files, `headless` tag reusing nop/headless backends, `PlatformPlugin` via DefaultPlugins, CGo gated to mobile), `l2-window-system-go` (Window component as ECS entity, diff-driven sync via change-detection, deferred create/destroy commands, `MainThreadExecutor`-bound `WindowBackend`, headless CI backend, `RawWindowHandle`→render surface). All `Draft` (L1 parents Draft → layer constraint). Chosen as the P6 dependency roots (error-core+platform have no intra-P6 deps; window depends on platform). 3 new hard L2→L1 edges (32 total), acyclic; window↔platform soft `Related` cycle non-blocking. INDEX v2.34.0 (104 specs, Stable 58/Draft 46). Deferred: ui/definition/diagnostic L2 (next tier); editor/plugin L2s gated on multi-repo Stable; compatibility-policy + examples-framework need no L2. Post-Update Review (Critic): Invariant Compliance substantive, contracts (no impl code), `Implements`/`Related` links valid, C-003 stdlib-first. **Handoff: `/magic.task`** to map specs into Phase 6 (clears 3 orphan warnings + sync gap).
- 2026-05-28 **`/magic.task` — P5 Stabilization (8 specs Draft → Stable)** — Pre-Planning Stabilization promoted **4 L1 + 4 L2** (audio, animation, tweening, 2d-rendering) after re-verifying the C29 P5 gate (`go test ./...` 46/46 PASS, all 4 examples hash-stable ×20). The 4 promoted L2 specs' **Canonical References populated** with on-disk-verified source + test + example files — deliberately closing the P4-style empty-refs advisory debt for P5. **`l1-asset-formats` + `l2-asset-formats-go` HELD Draft**: the spec's headline glTF multi-asset fan-out (INV-4), `.scene.json` codec, and font loaders have zero implementation (only stdlib image + WAV loaders landed); no end-to-end example in the C29 gate. C24 Skeptic cleared the hold — no Stable spec hard-depends on asset-formats (it is a downstream *consumer* of audio/animation, so no broken Stable→Draft edge), and all 4 promoted L2s now have Stable L1 parents (layer integrity intact). INDEX v2.33.0 (Stable 50 → 58, Draft 51 → 43), PLAN/TASKS v1.16.0. **Next:** Phase 6 stays `Hold` — author + stabilize missing P6 L2 contracts via `/magic.spec` before activation.
- 2026-05-28 **`/magic.run` — Phase 5 COMPLETE (18/18)** — **Tracks B/C/T04 Done**: **Track B** (`pkg/asset/formats/image/` StdlibImageLoader PNG/JPEG, `pkg/asset/formats/audio/` WAVLoader PCM decode + normalisation, PNG/WAV golden tests pass); **Track C** (`pkg/render/sprite/` Sprite/Anchor/Rect2D/TextureSlicer/SpriteMesh, `internal/render/sprite2d/` Sprite2DFeature+ExtractedSprite+SortSprites+BatchKey+PickSprite — headless, no GPU); **T-5T04** (`examples/2d/` sort-key hash stable ×20, 3 batches from 6 sprites, pick hit). **T-5T05 C29 P5 gate CLOSED**: `go test ./... → 46/46 PASS`; `go build ./examples/{audio,animation,tweening,2d}/... OK`; `modernize → clean`; C-003 stdlib-only. **Phase 5 = 18/18 Done**, total **106/106** 🎉
- 2026-05-28 **`/magic.run` — Phase 5 Tracks A/D/E + T-5T01/02/03/05 Done (11/18)** — Tracks executed: **A** (`pkg/audio/` + `internal/audio/` — source/components/sink, backend/driver/bus/effect, HeadlessBackend+Server, spatial attenuation/panning, ServiceRegistry); **D** (`pkg/animation/` + `internal/animation/` — clip/curve/target, components/graph, skeletal SkinData/ValidateSkin, SampleCurve sampler with Step/Linear/CubicSpline/Hermite + slerp); **E** (`pkg/tween/` + `internal/tween/` — Tween/EasingFn/Lerp generic, 11+ easing functions, AdvanceTween with Loop/PingPong/LoopOnce, writeAccessor reflection path). Validation: **T-5T01** audio hash ×20, **T-5T02** animation pose hash ×20, **T-5T03** tweening LoopMode/PingPong/despawn hash ×20, **T-5T05** C29 P5 gate OPEN (44/44 pkgs PASS, BenchmarkLerpVec3 **0 B/op 0 allocs/op**, C-003 stdlib-only, modernize clean). **Remaining:** T-5B (Asset Formats — joins A+D), T-5C (2D — Track C), T-5T04 (codec golden + 2D example). Race detector unavailable in this env (CGO/C-compiler missing); all tests pass without `-race`.
- 2026-05-28 **`/magic.task` — Phase 5 Activated** — `Ready` → **Active**. All unfreeze conditions met (render-core Stable + 5 L2 contracts + 18 tasks). Pre-Planning Stabilization: **0 promoted** — C29 P5 gate unmet (no `examples/{audio,animation,tweening,2d}/` yet); all 10 P5 specs stay `Draft [Bootstrap]`, consistent with P1–P4 (specs promote only after the validation track closes the gate). `phase-5.md` status Active; High-Level Checklist links L2. PLAN/TASKS v1.15.0, INDEX unchanged (v2.32.0). **Handoff: `/magic.run main`** — critical path {A‖D} → B → T. T-5T05 will close the gate → following `/magic.task` promotes P5 Draft → Stable.
- 2026-05-28 **`/magic.spec` — Phase 5 L2 contracts authored** — Created 5 new L2 Go specs: `l2-{audio-system,asset-formats,2d-rendering,animation-system,tweening-system}-go` (all Draft, each `Implements:` its L1 parent). Resolves the "P5 L1-only" gap → full L1+L2 parity with P1–P4. Each carries: Invariant Compliance table (L1→Go mapping), Go Package layout (`pkg/` public data + `internal/` logic), Type Definitions (Go contracts — permitted), Performance Strategy (C-027 0-alloc), Error Handling, Testing Strategy. Status Draft correct (parents Draft + no impl → Canonical Refs stub → Stable blocked). Key design choices: audio = headless stub driver default (C-003); asset-formats = stdlib-first + build-tag-gated optional codecs; 2d = `Sprite2DFeature` reuses render-core SoA; animation = reflection-cached property accessors + per-root parallel determinism; tween = single `any`-typed component + cached setter. INDEX v2.32.0 (101 specs, Stable 50/Draft 51), PLAN/TASKS v1.14.0. Post-Update Review (Critic): no impl code (Go contracts permitted), Invariant Compliance substantive, links valid.
- 2026-05-28 **`/magic.spec` — Render Core ratified Stable** — `l1-render-core` **RFC → Stable** + `l2-render-core-go` **Draft → Stable**, completing the ratification deferred in the prior `/magic.task`. Justification: Phase 4 complete + C29 gate (T-4T05); L1 Canonical Refs already filled + Q4/Q5 resolved; Q1–Q3 annotated non-blocking (forward-looking refinements — ref-counting tracker chosen per §4.6 and validated 0-alloc). L2 Canonical Refs populated with 11 source + 2 test files (all verified on disk via glob — Path Validity check). **Phase 4 = 10/10 Stable, RFC count → 0.** Phase 5 gate ("Render Core Stable") cleared → `Hold` → `Ready`, Track C (2D) unblocked. INDEX v2.31.0 (Stable 50/96), PLAN/TASKS v1.13.0. Post-Update Review (Critic): invariants intact, no impl code in spec (Go contracts permitted), links accurate. **Remaining recommended step:** author L2 Go contracts for the 5 P5 specs (still L1-only).
- 2026-05-28 **`/magic.task` — P4 Stabilization + P5 Decomposition** — Pre-Planning Stabilization promoted **8 P4 render specs Draft → Stable** (l1/l2 × mesh-and-image, materials-and-lighting, camera-and-visibility, post-processing) — C29 P4 gate closed by T-4T05. **Phase 4 → Done** (19/19). **User decision:** `l1-render-core` kept **RFC** + `l2-render-core-go` Draft (layer-blocked) — RFC→Stable ratification deferred to `/magic.spec`. This is a *promotion-quarantine* only — tasks T-4A01..04 are Done (C12.1 exception, nothing moved to Backlog). **Phase 5 full atomic decomposition** (18 tasks → `tasks/phase-5.md`): Track A:3 Audio / B:3 Asset Formats / C:2 2D / D:3 Animation / E:2 Tweening / T:5 Validation. Critical path **{A‖D} → B → T** (glTF/audio loaders consume AnimationClip + AudioSource types); Track E independent; **Track C externally gated** on render-core Stable. Skeptic flags: Animation (D) under-sizing risk; P5 specs are L1-only (L2 Go contracts pending /magic.spec); phase `Hold` condition over-broad (only Track C truly needs render-core). INDEX v2.30.0 (Stable 48/96), PLAN/TASKS v1.12.0.
- 2026-05-28 **PHASE 4 COMPLETE — T-4T01..05 Done** — Track T validation: `examples/3d/` (Cube+PBR+DirectionalLight+2-cascade shadow, frame hash stable 20 runs), `examples/camera/` (3-camera Order-sort determinism 20 runs), `examples/shader/` (post-process Bloom→Tonemap→FXAA, AA conflict detection, INV-3 golden topology). `internal/render/conformance_test.go` (recordingBackend, all 10 RenderBackend methods). `internal/render/isolation_test.go` (PostProcessStack value-copy isolation, slice-backing isolation, multi-frame isolation). T-4T05 C29 gate: 36/36 pkgs PASS; `go build ./examples/{3d,camera,shader}/...` OK; C-003 stdlib-only; BenchmarkFrustumCullSoA/ClusterLights/BuildPostChain/SpecKey all 0 B/0 allocs. P4 specs eligible Draft→Stable via next /magic.task.
- 2026-05-28 **Done: T-4E02 — Track E complete** — `pkg/render/postprocess/custom.go`: FullscreenMaterial{Shader Handle[material.Shader], InputTex []RID, Params map[string]ShaderValue, InsertAfter}. `internal/render/postpass/pingpong.go`: PingPongPool (2 HDR + 2 LDR pre-alloc RIDs, Reset=index-rewind C-027, 0-alloc), CheckAAConflict (FXAA+SMAA→ErrAAConflict, SMAA preferred). INV-4 proven by PostProcessStack value semantics. BenchmarkBuildPostChain 0 B/op 0 allocs/op. 24/24 PASS.
- 2026-05-28 **Done: T-4E01** — `pkg/render/postprocess/{stack,settings,tonemap,colorgrade}.go` + `internal/render/postpass/builder.go`: EffectSlot 10-slot iota enum (index=order, INV-2), IsHDR(), PostProcessStack Enable/Disable/EnabledSlots (canonical); per-effect settings structs; Tonemapper 5-op Apply() (Reinhard/ReinhardLuminance/ACES-Narkowicz/AgX-smoothstep/TonyMcMapface-fallback); ColorGrading; BuildPostChain([]EffectSlot→slices.Sort→validateOrder INV-1→postPass chain INV-3); ErrPostOrder/ErrAAConflict sentinels. 16/16 PASS; reference values ≤5e-4.
- 2026-05-28 **Done: T-4D03 — Track D complete** — `internal/render/lighting/{cluster,shadowpass}.go`: LightRef{Sphere,ShadowRID,Kind}, Froxel (pre-computed TileX/Y/Z), ClusterGrid + Reset() (C-027), ClusterLights (nil→sequential 0-alloc, pool→ForBatched disjoint-write); tileOverlapsLight (VP→clip→NDC projection, 2D circle-vs-AABB, directional fast-path); ShadowCaster, shadowMapPass (Outputs=[shadowRID]), lightingPass (Inputs=all shadow RIDs), BuildShadowPasses (graph edges → topo-sort enforces INV-4). BenchmarkClusterLights 0 B/op 0 allocs/op steady; parallel≡sequential; right-handed +Z behind-camera test. 7/7 PASS.
- 2026-05-28 **Done: T-4D02** — `pkg/render/light/{light,ibl,shadow}.go`: PointLight/SpotLight/DirectionalLight/AmbientLight (pure data, optional Shadow pointers); CubeShadow.FaceCount()=6, SingleShadow.MapCount()=1 (L1 §4.6 table); CascadeShadowConfig.Splits() — log: `near×(max/near)^(i/Count)` (INV-3 by construction), manual: INV-3 coverage guard → ErrCascadeCoverage, Count clamped 1..4; EnvironmentMapLight/IrradianceVolume (SH probes). Bootstrap: ManualSplits field (L2 spec `Splits` field renamed to avoid method/field name conflict). 14/14 tests PASS; go vet + modernize clean; C-003.
- 2026-05-28 **Done: T-4D01** — `pkg/render/material/{material,pbr,speckey}.go`: `AlphaMode` (5 variants) + `Phase()` total switch (INV-5); `MaterialParameters.Sanitize()` (idempotent clamp: metallic/roughness/occlusion→[0,1], all color components→≥0, slog.Debug on change); `Material` + `Validate()` (INV-1 ErrMaterialNoShader); `resolvePhase()` (PhaseHint boundary guard — isOpaqueSide rejects cross-bucket hints silently); `StandardPBR()` (white/dielectric/0.5rough); `SetFloat/SetColor/SetTexture` chaining setters; `SpecializationKey` + `SpecKey(VertexLayout)` → pipeline cache bridge; `assetIDHash` (fmt+FNV-1a; zero-ID→0, 0-alloc fast path). 13/13 tests PASS; `BenchmarkSpecKey` 0 B/op 0 allocs/op; go vet + modernize clean; C-003 stdlib+engine. Next: T-4D02 (Light components + IBL + shadow config).
- 2026-05-28 **Done: Tracks B + C** (5 tasks) — **Track B:** `pkg/render/mesh/{mesh,layout,primitives,skin}.go` + `pkg/render/image/{image,atlas,loaders}.go` + `internal/render/upload/staging.go`; Mesh INV-1/2/3 validated, FNV-1a VertexLayout deterministic hash, 6 primitives (Cube/Sphere/Plane/Cylinder/Capsule/Torus), shelf-pack DynamicAtlas INV-5, PNG/JPEG decode, C-027 StagingPool. **Track C:** `pkg/render/camera/{camera,projection,visibility,frustum,bundles}.go` + `internal/render/cameraupd/systems.go`; perspective/ortho Matrix() with ErrInvalidNearPlane/ErrDegenerateOrtho guards, FrustumFromViewProj (Gribb–Hartmann, inward-normal, normalised), 3-layer visibility (Visibility→InheritedVisibility→ViewVisibility), buildChildrenMap-based DFS propagation, ForBatched disjoint-index cull (10k parallel≡sequential), SortedActiveCameras (Order,EntityID). All tests PASS; `go vet` + modernize clean; C-003 stdlib+engine. D/E/T unblocked.
<!-- Phase 4 Track A (T-4A01..04, 2026-05-19) decisions archived → PLAN.md Document History v1.12.0 (Phase 4 Done). -->
<!-- Phase 1–3 decisions archived (Done; see PLAN.md Document History + archives/tasks/). -->
- 2026-04-30 **Pattern (carried):** ECS hot paths use `sync.Pool` with a `*SliceBuf[T]` wrapper for 0-alloc slice reuse; deferred mutations apply after all systems run, before tick boundary (single sync point). Reused by render: Server FIFO drain, ResourceTracker deferred-delete.

## Blockers

<!-- Empty if none. Format: [severity] description -->

<!-- [resolved 2026-05-28] render-core RFC blocker — ratified RFC → Stable via /magic.spec; Phase 5 gate cleared. -->
<!-- [resolved 2026-05-28] P5 L1-only blocker — 5 L2 Go contracts authored via /magic.spec; full L1+L2 parity. -->
- [advisory] **8 P4 render specs may have empty Canonical References.** Promoted via task.md relaxed-MVC (not spec.md strict checklist). For full spec.md compliance, populate `## Canonical References` on mesh/materials/camera/post L1+L2 (impl exists in `pkg/render/...`). Non-blocking; hygiene pass.
<!-- [resolved 2026-05-31] asset-formats held Draft → narrowed (Option A) + promoted Draft → Stable via /magic.task. Stable surface = glTF loader (INV-2/INV-4) + stdlib PNG/JPEG + WAV; HDR/DDS/KTX2/OGG/MP3/FLAC/fonts/`.scene.json` + glTF animations/skins/morph/KHR explicitly marked Planned (no overclaim). Canonical Refs populated. INDEX v2.39.0 (Stable 74). Optional follow-on: pkg/asset/formats/scene/ to graduate the scene.json Planned item. -->

## Blocking Constraints

<!-- Anti-patterns discovered through real failures. MANDATORY reading. -->
<!-- Agent MUST explicitly acknowledge each constraint before working. -->

- [C-001] **C29 Promotion Gate**: No P1 spec may be promoted Draft → Stable until `examples/ecs/poc/` validates the runtime end-to-end (T-1T05).
- [C-002] **STOP FACTOR (Phase ≥ 4 Hold)**: Phases 4–8 stay in `Hold` until Phase 1 POC is validated AND Phase 2 App Framework reaches `Stable`. No premature implementation work in those subsystems.
- [C-003] **C24 Stdlib Priority**: Engine core MUST have zero external Go deps. Any third-party package requires explicit justification recorded in an ADR.
- [C-004] **C27 GC Compensation**: Hot-path allocations (commands, events, transient views) MUST flow through `sync.Pool`. Validation Track verifies ≤1 alloc/op for `BenchmarkCommandFlush`.
- [C-005] **C28 Race Gate**: All concurrent tests MUST pass with `-race`; CI blocks otherwise.

## Session Continuity

**Last Session Ended:** 2026-05-31 (late)
**Handoff File:** none
**Bootstrap Mode:** true (P5 = 10/10 Stable — asset-formats narrowed → Stable; P6 engine-core cohort = 12 specs Stable; P6 tooling F-CLI + editor N/H/O/P cores + glTF loader implemented; P6 tooling E/I/J/L/M + editor-bridge wiring + P7/P8 remain `[Bootstrap]`)
